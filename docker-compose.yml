services:
  kami:
    build: .
    image: kami-paperbox:latest
    ports:
      - "8080:8080"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 8080
      # KAMI_PROXY: http://host.docker.internal:7890
    volumes:
      - kami-data:/app/.data
    restart: unless-stopped

volumes:
  kami-data:
