# 复制为 .env 后按需填写。不要把 .env 提交到 git。
# 本应用主要读 kami.config.json；环境变量可覆盖其中的项。

# HOST=0.0.0.0
# PORT=8080

# 上游代理（http / https / socks5）。设置页保存的地址优先。
# KAMI_PROXY=http://127.0.0.1:7890

# 数据面后端：设置后走 Neon（部署形态），留空用本地 PGlite（.data）。
# DATABASE_URL=postgres://user:pass@host/db

# 应用账号（better-auth）会话签名密钥；不设则本机进程内派生稳定密钥。
# BETTER_AUTH_SECRET=change-me

# 数据根目录（.data / vault / migrations 解析基准）；默认仓库内 .data。
# KAMI_ROOT=/absolute/path/outside/repo

# 登录中转浏览器的 Chrome 可执行文件（也认 KAMI_CHROME / CHROME_PATH）。
# PUPPETEER_EXECUTABLE_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe
