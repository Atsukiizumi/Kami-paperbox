# 系统架构图（分层视图）

**图的目的**：展示进程内部分层与调用方向，定位每层职责与「访问闸」位置。
**涉及的模块**：app 薄壳层、src/routes、components、lib 各层、持久化、外部出站。
**2026-09-11 复核**：数据面 10 路由已统一过 `withDataPlane` 访问闸（SEC-01 ✅）；upstream 已拆分为六模块（TD-01 ✅）。

```mermaid
flowchart TB
    subgraph shell["UI 壳层（app/ 薄壳）"]
        pages["app/**/page.tsx<br/>（use client 薄壳，11 页）"]
        layout["app/layout.tsx + providers.tsx<br/>AuthProvider › QueryClient › Theme › Tooltip<br/>+ AccountSyncBridge + AppShell"]
    end

    subgraph ui["UI 层（src/routes + src/components）"]
        routecomp["路由组件（index/search/work/pool/creator/user/vault/queue/history/rankings/settings）"]
        comp["业务组件（artwork-card / masonry-board / hover-preview / lightbox / ugoira-player / session-relay…）"]
        ui_lib["UI 基础（masonry-flow / motion / theme / card-aspect / paper-fly / media-lane / work-detail）"]
    end

    subgraph client["客户端应用层（src/lib 客户端）"]
        source_client["source.ts（zod schema + fetchSource/mutateSource）"]
        queue["queue-runner / save-work / persist-files / folder-access"]
        stores["zustand：useSettings / useQueue / useViewHistory / 词表目录"]
        browser_cache["browse-cache v2（localStorage，键含凭据指纹）· vault IDB · view-history"]
    end

    subgraph api["API 层（app/api → src/routes/api，withDataPlane 统一过闸）"]
        gateway["/api/source /api/social（op 分发）"]
        media_api["/api/media（图片代理）"]
        data_api["/api/vault /api/rankings /api/sessions /api/proxy<br/>/api/whoami /api/login-browser /api/reverse-search"]
        auth_api["/api/auth/*（Better Auth）· /api/account/sync（会话强制）"]
        dp["auth/data-plane.server.ts 访问闸<br/>Fetch-Metadata 同站校验 → 应用会话或 LAN 令牌"]
    end

    subgraph service["服务层（src/lib upstream/* + *.server.ts）"]
        upstream["upstream/dispatch + pixiv + fanbox + booru-sites<br/>（upstream.server.ts 桶导出，TD-01 拆分）"]
        booru_pixiv["booru.ts / pixiv-feed / pixiv-search / pixiv-profile"]
        social["social.server.ts（CSRF 自管）"]
        reverses["reverse-search.server.ts（3 引擎）"]
        login["browser-login.server.ts（puppeteer 单例）"]
        identity["site-identity.server.ts / session-cookies.server.ts"]
    end

    subgraph infra["基础设施层"]
        outbound["curl-fetch.server.ts outboundFetch<br/>undici ProxyAgent 池 → curl 兜底（3 败熔断 10min）"]
        gate["media-gate（并发闸+单飞）· throttle（热载限速）· cache-watermark（水位）"]
        proxycfg["proxy.server.ts（kami.config.json 热载）"]
        request_ctx["next-route.ts withRequest/withDataPlane（AsyncLocalStorage）"]
    end

    subgraph persist["持久化层"]
        sql["db.ts Sql 抽象<br/>PGlite（内存）⇄ Neon（pg）"]
        snapshot["db-snapshot.server.ts（JSON 快照 30s/500ms<br/>⚠ 不含 segments/meta，TD-19）"]
        vault_store["vault-store.server.ts（SQLite + files/，原子写）"]
        ranking_store["ranking-store.server.ts（SQLite）"]
        disk_cache["source-cache / media-cache（.data 文件）"]
        authz["auth/*（Better Auth 邮箱密码 + LAN 令牌）"]
    end

    pages --> routecomp
    layout --> routecomp
    routecomp --> comp & stores
    comp --> ui_lib
    routecomp --> source_client & queue & browser_cache
    source_client -->|"POST /api/source · /api/social"| dp
    queue -->|"fetch /api/media"| dp
    dp --> gateway & media_api & data_api
    gateway --> upstream
    media_api --> upstream
    data_api --> login & identity & vault_store & ranking_store & proxycfg
    auth_api --> authz
    upstream --> booru_pixiv & outbound
    social & reverses & login --> outbound
    upstream --> disk_cache & gate
    outbound --> proxycfg
    authz --> sql
    sql --> snapshot
    vault_store & ranking_store & disk_cache & snapshot --> fs[("本地磁盘 .data/")]

    style dp stroke:#b8860b,stroke-width:2px
    style snapshot stroke-dasharray: 5 5
```

**图解说明**

- **`dp`（数据面访问闸）横切全部数据面 API**（2026-09-10 起）：每个 Route Handler 被 `withDataPlane` 包装——先 Fetch-Metadata 同站校验（跨站 scripted 请求 403），再要求应用账号会话（authConfigured 时）或 LAN 启动令牌（关闭态三通道：Authorization 头 / `?token=` / 配对 cookie）。`/api/account/sync` 在闸内再强制 Better Auth 会话。
- `request_ctx` 横切全部 API 层：Route Handler 被 `withRequest` 包装后，Request 进入 AsyncLocalStorage，认证与 cookie 读取不依赖参数传递。
- 持久化四轨并行：SQL 轨（账号/同步）、SQLite 轨×2（纸匣/热榜）、文件轨（缓存/快照）——各自独立，无统一 Repository。
- PGlite 是内存库，进程重启即空，靠 `snapshot` 落 `.data/kami-db-snapshot.json` 恢复（user/account/session/user_sync 四表 + `.data/auth-secret.json` 签名密钥）；**TD-19**：user_sync_segments/user_sync_meta 不在清单内。
- 浏览器侧另有 `media-lane`（4 条图片加载车道，详情首图插队），与服务端 media-gate 构成两层并发闸。

**风险点 / 注意事项**：~~upstream 单文件瓶颈（TD-01）~~ 已拆分；`media_api` 直接深达 upstream（不经 gateway 的 op 分发），新增出站逻辑时注意不要绕开统一校验；新增数据面端点必须走 `withDataPlane`（09 指南）。
