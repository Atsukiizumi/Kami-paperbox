# 系统架构图（分层视图）

**图的目的**：展示进程内部分层与调用方向，定位每层职责与「鉴权断点」。
**涉及的模块**：app 薄壳层、src/routes、components、lib 各层、持久化、外部出站。

```mermaid
flowchart TB
    subgraph shell["UI 壳层（app/ 薄壳）"]
        pages["app/**/page.tsx<br/>（use client 薄壳，11 页）"]
        layout["app/layout.tsx + providers.tsx<br/>AuthProvider › QueryClient › Theme › Tooltip<br/>+ AccountSyncBridge + PreviewHostBridge"]
    end

    subgraph ui["UI 层（src/routes + src/components）"]
        routecomp["路由组件（index/search/work/pool/creator/user/vault/queue/history/rankings/settings）"]
        comp["业务组件（artwork-card / masonry-board / hover-preview / lightbox / ugoira-player / session-relay…）"]
        ui_lib["UI 基础（masonry-flow / motion / theme / card-aspect / paper-fly）"]
    end

    subgraph client["客户端应用层（src/lib 客户端）"]
        source_client["source.ts（zod schema + fetchSource/mutateSource）"]
        queue["queue-runner / save-work / persist-files / folder-access"]
        stores["zustand：useSettings / useQueue / useViewHistory / 词表目录"]
        browser_cache["browse-cache（localStorage）· vault IDB · view-history"]
    end

    subgraph api["API 层（app/api → src/routes/api）"]
        gateway["/api/source /api/social（op 分发）"]
        media_api["/api/media（图片代理）"]
        data_api["/api/vault /api/rankings /api/sessions /api/proxy<br/>/api/whoami /api/login-browser /api/reverse-search"]
        auth_api["/api/auth/*（Better Auth）· /api/account/sync（唯一强制鉴权）"]
    end

    subgraph service["服务层（src/lib *.server.ts）"]
        upstream["upstream.server.ts（1257 行站点适配 + 媒体代理）"]
        booru_pixiv["booru.ts / pixiv-feed / pixiv-search / pixiv-profile"]
        social["social.server.ts（CSRF 自管）"]
        reverses["reverse-search.server.ts（3 引擎）"]
        login["browser-login.server.ts（puppeteer 单例）"]
        identity["site-identity.server.ts / session-cookies.server.ts"]
    end

    subgraph infra["基础设施层"]
        outbound["curl-fetch.server.ts outboundFetch<br/>undici ProxyAgent 池 → curl 兜底（3 败熔断 10min）"]
        gate["media-gate（并发闸）· throttle（热载限速）"]
        proxycfg["proxy.server.ts（kami.config.json 热载）"]
        request_ctx["next-route.ts withRequest（AsyncLocalStorage）"]
    end

    subgraph persist["持久化层"]
        sql["db.ts Sql 抽象<br/>PGlite（内存）⇄ Neon（pg）"]
        snapshot["db-snapshot.server.ts（JSON 快照 30s/500ms）"]
        vault_store["vault-store.server.ts（SQLite + files/）"]
        ranking_store["ranking-store.server.ts（SQLite）"]
        disk_cache["source-cache / media-cache（.data 文件）"]
        authz["auth/*（Better Auth 三态 + Gate JWT）"]
    end

    pages --> routecomp
    layout --> routecomp
    routecomp --> comp & stores
    comp --> ui_lib
    routecomp --> source_client & queue & browser_cache
    source_client -->|"POST /api/source · /api/social"| gateway
    queue -->|"fetch /api/media"| media_api
    gateway --> upstream
    media_api --> upstream
    data_api --> login & identity & vault_store & ranking_store & proxycfg
    auth_api --> authz
    upstream --> booru_pixiv & outbound
    social & reverses & login --> outbound
    upstream --> disk_cache & gate
    outbound --> proxycfg
    authz --> sql
    sql --> snapshot
    vault_store & ranking_store & disk_cache & snapshot --> fs[("本地磁盘 .data/")]

    style authz stroke-dasharray: 5 5
    style data_api stroke-dasharray: 5 5
```

**图解说明**

- 实线为主数据流；`gateway` 与 `data_api` 之间的差异值得注意：**除 `/api/account/sync` 外，data_api 一组（虚线框）均无鉴权**——authz 模块（虚线）已实现 `runAuth` 但未接入任何 data_api 端点（SEC-01）。
- `request_ctx` 横切全部 API 层：Route Handler 被 `withRequest` 包装后，Request 进入 AsyncLocalStorage，认证与 cookie 读取不依赖参数传递。
- 持久化四轨并行：SQL 轨（账号/同步）、SQLite 轨×2（纸匣/热榜）、文件轨（缓存/快照）——各自独立，无统一 Repository。
- PGlite 是内存库，进程重启即空，靠 `snapshot` 落 `.data/kami-db-snapshot.json` 恢复。

**风险点 / 注意事项**：上游适配集中在 `upstream.server.ts` 单文件（图中 service 层最大节点），是演进的主要瓶颈（TD-01）；`media_api` 直接深达 `upstream`（不经 gateway），新增出站逻辑时容易绕开统一校验。
