# 数据流图（DFD）

**图的目的**：追踪三类核心数据（列表 JSON、图像像素、凭据）在存储之间的流动与落点。
**涉及的模块**：M1/M2/M3/M5/M7/M8 与各存储。2026-09-11 复核：凭据流已加密化（SEC-03/04 ✅），标红仅剩 localStorage 明文残余。

```mermaid
flowchart LR
    subgraph browser_stores["浏览器存储"]
        ls_set["localStorage<br/>kami-settings（含明文 cookie）"]
        ls_browse["localStorage<br/>kami-browse-v2（键含凭据指纹）"]
        ls_q["localStorage<br/>kami-queue / kami-history / 词表"]
        ss_kek["sessionStorage<br/>kami-sync-kek（关标签即失）"]
        idb_vault["IndexedDB kami-vault<br/>meta + 像素 blobs"]
        idb_folder["IndexedDB kami-folder<br/>目录句柄"]
        rq["react-query 内存<br/>（30min stale / 2h gc）"]
    end

    subgraph server_stores["服务端存储（.data/）"]
        src_cache["source/*.json<br/>（30min，水位 512MB）"]
        media_cache["media/*.bin+json<br/>（7d，≤8MB/文件，水位 2GB）"]
        vault_db["vault/vault.sqlite<br/>+ vault/files/"]
        rank_db["rankings/rankings.sqlite"]
        dbsnap["kami-db-snapshot.json<br/>（四表全量，⚠ 不含 segments TD-19）"]
        usertable[("PGlite user_sync_segments<br/>（settings 段可密文）")]
        proxyjson["proxy.json + kami.config.json"]
    end

    upstream["图站上游"]
    page["页面组件"]
    api["本站 API（withDataPlane 过闸）"]

    ls_browse -->|"启动同步 hydrate"| rq
    rq -->|"dehydrate（防抖，16 查询×8 页）"| ls_browse
    page <--> rq
    page -->|"POST op + cookie(body)"| api
    api <--> src_cache
    api -->|"miss 时"| upstream
    upstream -->|"列表 JSON"| api
    api -->|"FetchOk"| rq

    page -->|"GET /api/media?u=（经 media-lane 车道）"| api
    api <--> media_cache
    media_cache -.->|"miss"| upstream

    page -->|"PUT /api/vault（multipart）"| api
    api --> vault_db
    idb_vault <-->|"封面读取：本地优先 miss 打 API"| page
    page -->|"归档写"| idb_vault
    idb_folder -->|"目录写 + 巡检 SHA-256"| page

    api --> rank_db
    ls_set -->|"collectBackup（settings 段）"| seg["分段载荷 ≤2MB/段<br/>cipher 或 credsOmitted"]
    ls_q --> seg
    idb_vault -->|"目录段（无像素）"| seg
    ss_kek -->|"KEK 加密 settings 段"| seg
    seg -->|"POST /api/account/sync（鉴权）"| usertable
    usertable -.->|"⚠ TD-19：断链"| dbsnap
    page -->|"POST /api/proxy"| proxyjson

    style ls_set stroke:#c0392b,color:#c0392b
    style dbsnap stroke:#b8860b,color:#b8860b
```

**图解说明（2026-09-11 复核）**

- **列表数据**走「上游 → API → 盘缓存 → react-query → localStorage」单向流，每层有独立 TTL（30min/1800s/30min/24h）；browse 缓存键与 react-query 键均含**凭据指纹**（不可逆，SEC-04 ✅——旧的带 Cookie 原文的 v1 存储读到即删）。
- **像素数据**只在归档时进 IDB/磁盘；浏览态像素始终由 `/api/media` 代理（浏览器 HTTP 缓存 7d immutable），浏览器侧经 media-lane 4 条车道限并发。
- **凭据流**（SEC-03 ✅ 后）：localStorage `kami-settings` 仍是明文（标红，长期残余）；离开浏览器后全程密文——settings 段以 KEK（PBKDF2(账号密码, 服务端 salt)，只存 sessionStorage）加密落 user_sync_segments；备份导出可选口令加密（v2）。
- 橙色标出的 `dbsnap` 断链是 **TD-19**：分段表数据不会进快照，PGlite 形态重启即丢。

**风险点 / 注意事项**：`kami-settings` 明文是 XSS/本机进程攻击面（单机信任模型下接受，长期方案收敛到服务端 HttpOnly）；TD-19 修复前，重启后靠「有变更的浏览器重新推送分段」自愈。
