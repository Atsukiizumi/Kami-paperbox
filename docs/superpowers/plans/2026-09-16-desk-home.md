# 今日案头（主页）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/` 换成稿纸案头；现有浏览整页搬到 `/browse`，保活、返回浏览、空态链接一并改指。

**Architecture:** 壳把浏览保活从 `pathname === "/"` 改挂 `/browse`；案头是独立 `DeskPage`，只读已有数据（日榜 `fetchSource`、追踪名单、watch-badge、纸匣 + 浏览历史）。未读是纯函数，不新表。报纸用独立 queryKey `desk-newspaper`，禁止复用 `home-pixiv` / `home-booru`。

**Tech Stack:** 现有 Next App Router、TanStack Query、zustand、lucide `BookOpen`、node:test。无新依赖。

**Spec:** `docs/superpowers/specs/2026-09-15-desk-home-design.md`

## Global Constraints

- 无新依赖、无新表、无新同步段、无新设置字段。
- 案头 **禁止** 调用 `checkWatchArtists`。
- 手机底栏仍六格；`NAV` 的 `/`（案头）、`/queue`、`/watch` 不进 `MOBILE_NAV`。
- `/`、`/browse`、`/watch` 都不进 `isMainNavPath`（案头不卸浏览保活）。
- 去浏览纯字无图；报纸 4 张竖图，不用 `ArtworkCard`；失败/空/FANBOX 整栏不渲染、不 toast。
- 色彩只用既有 token；只有去浏览一块 `.kami-card-folded`；不新 keyframes。
- 文件头注释写清作用 / 用法 / 为什么。
- 站点切换：已在 `/` 或 `/browse` 只 `setTab`；其它页跳 `/browse`（旧行为是跳当时的首页网格）。点标签搜图（`setBrowseQuery`）必须 `navigate("/browse")`。
- Logo、新手引导完成 → `/`（案头）。详情「返回浏览」、空态「去浏览」、`searchTag` → `/browse`。
- 测试：`node --experimental-strip-types --test <file>`；全量 `pnpm test`。commit：`git -c commit.gpgsign=false commit --no-gpg-sign`。验证命令必须看退出码。

## File map

| 文件 | 职责 |
| --- | --- |
| `src/lib/desk-unread.ts` | `workKey` / `unreadItems` 纯函数 |
| `src/lib/desk-unread.test.ts` | 未读 90 天窗与 viewedAt 边界 |
| `src/lib/desk-newspaper.ts` | 从 `FetchOk` 抽出最多 4 张 |
| `src/lib/desk-newspaper.test.ts` | op 过滤与截断 |
| `src/lib/route-shape.ts` | `isBrowsePath` / `isDeskPath` / `navItemActive` |
| `src/lib/route-shape.test.ts` | 高亮与保活判定 |
| `src/routes/browse.tsx` | 现 `src/routes/index.tsx` 迁过来（`export function Home`） |
| `src/routes/desk.tsx` | 稿纸页：页眉、去浏览、笺条、拼装三块 |
| `src/components/desk/newspaper.tsx` | 今日报纸 |
| `src/components/desk/letters.tsx` | 未拆信 |
| `src/components/desk/stack.tsx` | 未读纸叠 |
| `app/page.tsx` | 渲染 `DeskPage` |
| `app/browse/page.tsx` | 渲染 `Home`（壳保活时 children 隐藏） |
| `src/components/app-shell.tsx` | NAV、保活、PageFrame |
| `src/lib/kami-link.tsx` | `KNOWN_ROUTE_PATHS` 加 `/browse` |
| 若干「去浏览」链接、e2e、`src/routes/vault.tsx`、CHANGELOG | 深链与回归 |

---

### Task 1: 未读判定纯函数

**Files:**
- Create: `src/lib/desk-unread.ts`
- Test: `src/lib/desk-unread.test.ts`

**Interfaces:**
- Consumes: `VaultMeta`（`src/lib/types.ts`）、`HistoryEntry` / `historyCutoff`（`src/lib/view-history.ts`，`HISTORY_DAYS = 90`）
- Produces:
  - `workKey(source: string, id: string): string` → `` `${source}:${id}` ``
  - `unreadItems(vault: readonly VaultMeta[], history: readonly HistoryEntry[], now?: number): VaultMeta[]`

未读当且仅当：`savedAt >= historyCutoff(now)` **且**（历史无该键 **或** `viewedAt < savedAt`）。更早藏品当未知，不进列表。`viewedAt === savedAt` 算已读。

- [ ] **Step 1: Write the failing test**

```ts
import assert from "node:assert/strict";
import { test } from "node:test";
import { HISTORY_DAYS, type HistoryEntry } from "../view-history.ts";
import type { VaultMeta } from "../types.ts";
import { unreadItems, workKey } from "./desk-unread.ts";

const DAY = 24 * 60 * 60_000;

function meta(over: Partial<VaultMeta> & Pick<VaultMeta, "id" | "savedAt">): VaultMeta {
  return {
    key: `pixiv:${over.id}`,
    source: "pixiv",
    title: over.id,
    author: "a",
    authorId: "",
    tags: [],
    pageCount: 1,
    bytes: 1,
    ...over,
  };
}

function hist(over: Partial<HistoryEntry> & Pick<HistoryEntry, "id" | "viewedAt">): HistoryEntry {
  return {
    source: "pixiv",
    title: over.id,
    author: "",
    authorId: "",
    thumb: "",
    pageCount: 1,
    ...over,
  };
}

test("workKey 是 source:id", () => {
  assert.equal(workKey("yande", "9"), "yande:9");
});

test("从未打开且在 90 天内 → 未读", () => {
  const now = Date.UTC(2026, 8, 16);
  const got = unreadItems([meta({ id: "1", savedAt: now - 3 * DAY })], [], now);
  assert.equal(got.length, 1);
  assert.equal(got[0]?.id, "1");
});

test("打开晚于收藏 → 已读", () => {
  const now = Date.UTC(2026, 8, 16);
  const savedAt = now - 2 * DAY;
  const got = unreadItems(
    [meta({ id: "1", savedAt })],
    [hist({ id: "1", viewedAt: savedAt + 1000 })],
    now,
  );
  assert.equal(got.length, 0);
});

test("打开早于收藏 → 未读", () => {
  const now = Date.UTC(2026, 8, 16);
  const savedAt = now - 2 * DAY;
  const got = unreadItems(
    [meta({ id: "1", savedAt })],
    [hist({ id: "1", viewedAt: savedAt - 1000 })],
    now,
  );
  assert.equal(got.length, 1);
});

test("viewedAt 等于 savedAt → 已读", () => {
  const now = Date.UTC(2026, 8, 16);
  const savedAt = now - DAY;
  const got = unreadItems(
    [meta({ id: "1", savedAt })],
    [hist({ id: "1", viewedAt: savedAt })],
    now,
  );
  assert.equal(got.length, 0);
});

test("刚好在 90 天边界上（savedAt === cutoff）→ 未读", () => {
  const now = Date.UTC(2026, 8, 16);
  const cut = now - HISTORY_DAYS * DAY;
  const got = unreadItems([meta({ id: "1", savedAt: cut })], [], now);
  assert.equal(got.length, 1);
});

test("早于 90 天窗 → 未知，不进纸叠", () => {
  const now = Date.UTC(2026, 8, 16);
  const cut = now - HISTORY_DAYS * DAY;
  const got = unreadItems([meta({ id: "1", savedAt: cut - 1 })], [], now);
  assert.equal(got.length, 0);
});

test("空库空历史 → []；历史有键但匣里没有 → []", () => {
  const now = Date.UTC(2026, 8, 16);
  assert.deepEqual(unreadItems([], [], now), []);
  assert.deepEqual(unreadItems([], [hist({ id: "9", viewedAt: now })], now), []);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --experimental-strip-types --test src/lib/desk-unread.test.ts`

Expected: FAIL，`Cannot find module` / `desk-unread.ts` 不存在。

- [ ] **Step 3: Write minimal implementation**

`src/lib/desk-unread.ts`：

```ts
/**
 * 纸匣未读（案头纸叠）。
 *
 * 作用：藏品在 90 天浏览历史窗内、且收下之后没打开过，算未读。
 * 用法：unreadItems(listVault(), useViewHistory.getState().items)。
 * 为什么：不往 VaultMeta 加 openedAt；历史超过 90 天已剪枝，更早的当未知。
 */
import type { VaultMeta } from "./types.ts";
import { historyCutoff, type HistoryEntry } from "./view-history.ts";

export function workKey(source: string, id: string): string {
  return `${source}:${id}`;
}

export function unreadItems(
  vault: readonly VaultMeta[],
  history: readonly HistoryEntry[],
  now = Date.now(),
): VaultMeta[] {
  const cut = historyCutoff(now);
  const viewed = new Map<string, number>();
  for (const row of history) viewed.set(workKey(row.source, row.id), row.viewedAt);
  return vault.filter((item) => {
    if (item.savedAt < cut) return false;
    const seen = viewed.get(workKey(item.source, item.id));
    return seen === undefined || seen < item.savedAt;
  });
}
```

- [ ] **Step 4: Run tests and make sure they pass**

Run: `node --experimental-strip-types --test src/lib/desk-unread.test.ts`

Expected: PASS，exit 0。

- [ ] **Step 5: Commit**

```bash
git add src/lib/desk-unread.ts src/lib/desk-unread.test.ts
git -c commit.gpgsign=false commit --no-gpg-sign -m "feat: 纸匣未读判定（90 天窗，供案头纸叠）"
```

---

### Task 2: 路径谓词

**Files:**
- Modify: `src/lib/route-shape.ts`
- Test: `src/lib/route-shape.test.ts`

**Interfaces:**
- Consumes: 现有 `isWorkPath` / `isDetailPath` / `isMainNavPath`（`isMainNavPath` 本刀不把 `/`、`/browse`、`/watch` 加进去）
- Produces:
  - `isDeskPath(pathname: string): boolean` → `pathname === "/"`
  - `isBrowsePath(pathname: string): boolean` → `pathname === "/browse"`
  - `navItemActive(pathname: string, to: string): boolean`

`navItemActive` 规则（从 app-shell 抽出来）：

- `to === "/"` → 仅 `pathname === "/"`
- `to === "/browse"` → `pathname === "/browse"` 或 pathname 以 `/work`、`/user`、`/creator` 开头（与现「浏览高亮详情」相同，**不含** `/pool`）
- 其它 → `pathname === to` 或 `pathname.startsWith(to + "/")`

- [ ] **Step 1: Write the failing test**

```ts
import assert from "node:assert/strict";
import { test } from "node:test";
import { isBrowsePath, isDeskPath, isMainNavPath, navItemActive } from "./route-shape.ts";

test("isDeskPath / isBrowsePath", () => {
  assert.equal(isDeskPath("/"), true);
  assert.equal(isDeskPath("/browse"), false);
  assert.equal(isBrowsePath("/browse"), true);
  assert.equal(isBrowsePath("/browse/"), false);
  assert.equal(isBrowsePath("/"), false);
});

test("案头与浏览都不算 isMainNavPath（保活不被卸）", () => {
  assert.equal(isMainNavPath("/"), false);
  assert.equal(isMainNavPath("/browse"), false);
  assert.equal(isMainNavPath("/watch"), false);
  assert.equal(isMainNavPath("/vault"), true);
  assert.equal(isMainNavPath("/rankings"), true);
});

test("navItemActive：案头只亮自己", () => {
  assert.equal(navItemActive("/", "/"), true);
  assert.equal(navItemActive("/browse", "/"), false);
  assert.equal(navItemActive("/work/pixiv/1", "/"), false);
});

test("navItemActive：浏览亮网格和作品/画师/创作者，不亮合集", () => {
  assert.equal(navItemActive("/browse", "/browse"), true);
  assert.equal(navItemActive("/work/yande/1", "/browse"), true);
  assert.equal(navItemActive("/user/2", "/browse"), true);
  assert.equal(navItemActive("/creator/3", "/browse"), true);
  assert.equal(navItemActive("/pool/yande/4", "/browse"), false);
  assert.equal(navItemActive("/", "/browse"), false);
});

test("navItemActive：其它项前缀", () => {
  assert.equal(navItemActive("/vault", "/vault"), true);
  assert.equal(navItemActive("/vault/stats", "/vault"), true);
  assert.equal(navItemActive("/settings", "/vault"), false);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --experimental-strip-types --test src/lib/route-shape.test.ts`

Expected: FAIL（新导出不存在）。

- [ ] **Step 3: Write minimal implementation**

在 `src/lib/route-shape.ts` 追加（`isMainNavPath` 函数体不动）：

```ts
export function isDeskPath(pathname: string) {
  return pathname === "/";
}

export function isBrowsePath(pathname: string) {
  return pathname === "/browse";
}

export function navItemActive(pathname: string, to: string) {
  if (to === "/") return pathname === "/";
  if (to === "/browse") {
    return (
      pathname === "/browse" ||
      pathname.startsWith("/work") ||
      pathname.startsWith("/user") ||
      pathname.startsWith("/creator")
    );
  }
  return pathname === to || pathname.startsWith(`${to}/`);
}
```

文件头「用法」补一句：壳导航高亮用 `navItemActive`。

- [ ] **Step 4: Run tests**

Run: `node --experimental-strip-types --test src/lib/route-shape.test.ts`

Expected: PASS，exit 0。

- [ ] **Step 5: Commit**

```bash
git add src/lib/route-shape.ts src/lib/route-shape.test.ts
git -c commit.gpgsign=false commit --no-gpg-sign -m "feat: 案头/浏览路径谓词与导航高亮"
```

---

### Task 3: `/` 换案头骨架，浏览搬到 `/browse`

本任务结束后：打开应用能看见「今日」和「去浏览」；点去浏览进瀑布流；e2e 不再在 `/` 上等 `article`。报纸/信/纸叠下一刀再挂。

**Files:**
- Rename: `src/routes/index.tsx` → `src/routes/browse.tsx`（`git mv`，保留 `export function Home`）
- Create: `src/routes/desk.tsx`、`app/browse/page.tsx`
- Modify: `app/page.tsx`、`src/components/app-shell.tsx`、`src/lib/kami-link.tsx`（`KNOWN_ROUTE_PATHS` 加 `"/browse"`）、`src/components/back-to-browse.tsx`、`src/components/not-found.tsx`、`src/components/empty-sheet.tsx`（注释里的 `/` 改 `/browse`）、`src/components/site-switcher.tsx`、`src/components/use-card-interactions.ts`（`searchTag` → `/browse`）、`src/routes/work.$source.$id.tsx`（`searchTag` → `/browse`）、`src/routes/pool.$site.$id.tsx`、`src/routes/rankings.tsx`、`src/routes/vault.tsx`、`src/routes/vault-stats.tsx`、`src/routes/vault-report.tsx`、`e2e/critical-path.spec.ts`、`e2e/guest-tier.spec.ts`、`e2e/browse-cache.spec.ts`
- Do not change: `src/components/onboarding.tsx` 的 `navigate({ to: "/" })`、`app-shell` Logo 的 `to="/"`（落地案头）

**Interfaces:**
- Consumes: `isBrowsePath`、`isDeskPath`、`navItemActive`、`isMainNavPath`、`isDetailPath`、`isWorkPath`
- Produces: `DeskPage`（`src/routes/desk.tsx` `export function DeskPage`）；浏览仍 `export function Home`

- [ ] **Step 1: git mv 浏览模块**

```bash
git mv src/routes/index.tsx src/routes/browse.tsx
```

文件头「用法」改成：侧栏「浏览」；壳保活挂在 `/browse`。全文不要再写「首页就是浏览」。

- [ ] **Step 2: 壳保活改挂 /browse**

`app-shell.tsx`：

1. lucide 增加 `BookOpen`。
2. `NAV` 最前插入 `{ to: "/", label: "案头", icon: BookOpen }`；原浏览项改为 `{ to: "/browse", label: "浏览", icon: Compass }`。
3. `MOBILE_NAV = NAV.filter((item) => item.to !== "/" && item.to !== "/queue" && item.to !== "/watch")`。
4. 删掉本地 `isActive`，改 `import { isBrowsePath, isDeskPath, isDetailPath, isMainNavPath, isWorkPath, navItemActive } from "@/lib/route-shape"`，高亮用 `navItemActive(pathname, item.to)`。
5. dynamic：`import("@/routes/browse").then((m) => m.Home)`。
6. 保活：

```ts
const isBrowse = isBrowsePath(pathname);
const isDesk = isDeskPath(pathname);
const overlay = isDetailPath(pathname);
const [keepBrowse, setKeepBrowse] = useState(isBrowse);
const browseScroll = useRef(0);
const wasBrowse = useRef(isBrowse);

useEffect(() => {
  if (isBrowse) setKeepBrowse(true);
  if (isMainNavPath(pathname)) setKeepBrowse(false);
}, [isBrowse, pathname]);

useLayoutEffect(() => {
  if (wasBrowse.current && !isBrowse) browseScroll.current = window.scrollY;
  if (!wasBrowse.current && isBrowse) window.scrollTo(0, browseScroll.current);
  wasBrowse.current = isBrowse;
}, [isBrowse]);

const mountBrowse = isBrowse || ((overlay || isDesk) && keepBrowse);
```

7. `PageFrame` 入参把 `isHome` 换成 `isBrowse`：

```tsx
{mountBrowse ? (
  <div hidden={!isBrowse} aria-hidden={!isBrowse} className={isBrowse ? "pt-6 md:pt-8" : "hidden"}>
    <BrowsePage />
  </div>
) : null}
{detail && !isWorkPath(pathname) ? <DetailNav /> : null}
{!isBrowse ? (
  <div ref={ref} className={detail ? "pt-4 md:pt-5" : "pt-6 md:pt-8"}>
    {children}
  </div>
) : null}
```

冷打开 `/` 时 `keepBrowse === false`，不挂 `BrowsePage`（浏览大 chunk 不下载）。从 `/browse` 点 Logo 回案头时 `keepBrowse` 仍 true，浏览藏着。

- [ ] **Step 3: DeskPage 骨架 + 路由壳**

`src/routes/desk.tsx`：

```tsx
/**
 * 今日案头（主页稿纸）。
 *
 * 作用：打开应用落到这里；去浏览进 /browse。
 * 用法：app/page.tsx 渲染 DeskPage。
 * 为什么：浏览是工作面，案头是坐下的那张纸。
 */
"use client";

import { Link } from "@/lib/kami-link";
import { siteLabel } from "@/lib/sites";
import { useSettings } from "@/lib/store";
import { cn } from "@/lib/utils";

function todayLabel(now = new Date()): { title: string; date: string } {
  const date = new Intl.DateTimeFormat("zh-CN", {
    year: "numeric",
    month: "long",
    day: "numeric",
    weekday: "long",
  }).format(now);
  return { title: "今日", date };
}

export function DeskPage() {
  const tab = useSettings((s) => s.tab);
  const { title, date } = todayLabel();
  return (
    <div className="mx-0 max-w-6xl space-y-4">
      <header className="flex flex-wrap items-baseline gap-x-3 gap-y-1">
        <h1 className="font-display text-3xl tracking-tight md:text-4xl">{title}</h1>
        <p className="text-sm text-muted">{date}</p>
      </header>
      <Link
        to="/browse"
        className={cn(
          "kami-card-folded relative block min-h-[17.5rem] rounded-xl bg-surface p-6 shadow-[var(--shadow-paper-1)] md:p-8",
        )}
      >
        <p className="text-xs tracking-wide text-subtle">当前 · {siteLabel(tab)}</p>
        <p className="mt-6 font-display text-4xl tracking-tight md:text-5xl">去浏览</p>
        <p className="mt-3 text-sm text-muted">日榜、关注、推荐都在那边</p>
      </Link>
    </div>
  );
}
```

`app/page.tsx` 改渲染 `DeskPage`。`app/browse/page.tsx` 按 `app/history/page.tsx` 的写法：`"use client"; import { Home } from "@/routes/browse"; export default Home;`

`kami-link.tsx` 的 `KNOWN_ROUTE_PATHS` 在 `"/"` 后插入 `"/browse"`。

- [ ] **Step 4: 所有「语义是浏览」的链接改 `/browse`**

| 文件 | 改法 |
| --- | --- |
| `back-to-browse.tsx` | `Link to="/browse"`；`router.push("/")` fallback → `router.push("/browse")`；注释「回到首页网格」改「回到 /browse」 |
| `not-found.tsx` | `Link to="/browse"`，「返回浏览」文案保留 |
| `site-switcher.tsx` | 注释改为回到浏览工作面。`choose`：`if (pathname !== "/" && pathname !== "/browse") void navigate({ to: "/browse" })` |
| `use-card-interactions.ts` `searchTag` | `navigate({ to: "/browse" })` |
| `work.$source.$id.tsx` `searchTag` | 同上 |
| `pool.$site.$id.tsx`、`rankings.tsx` | 「返回浏览」→ `/browse` |
| `vault.tsx` / `vault-stats.tsx` / `vault-report.tsx` | EmptySheet「去浏览」→ `/browse` |
| `empty-sheet.tsx` 文件头用法示例 | `Link to="/browse"` |

不要改 Logo、不要改 onboarding 完成跳转。

- [ ] **Step 5: e2e 改指 /browse**

三处 `page.goto("/")` 且随后等瀑布流 `article` 的，改 `goto("/browse")`：

- `e2e/critical-path.spec.ts` 约第 43 行
- `e2e/guest-tier.spec.ts` 约第 21 行
- `e2e/browse-cache.spec.ts` 约第 57 行（注释「回浏览页」）

`critical-path` 在 `registerIfNeeded` 之后、进 `/browse` 之前加一段案头冒烟（冷开 `/`）：

```ts
await page.goto("/", { waitUntil: "domcontentloaded" });
await expect(page.getByRole("heading", { name: "今日" })).toBeVisible({ timeout: 30_000 });
await expect(page.getByRole("link", { name: /去浏览/ })).toBeVisible();
```

然后再 `goto("/browse")` 走原瀑布流断言。

- [ ] **Step 6: 跑谓词测试 + typecheck**

```bash
node --experimental-strip-types --test src/lib/route-shape.test.ts src/lib/desk-unread.test.ts
pnpm typecheck
```

Expected: 全 exit 0。若 `app-shell` 仍引用 `@/routes/index` 或 `isHome`，typecheck 会红，当场修。

- [ ] **Step 7: Commit**

```bash
git add -A
git -c commit.gpgsign=false commit --no-gpg-sign -m "feat: 案头占首页，浏览搬到 /browse"
```

确认没有把 `docs/` 加进去（gitignore）。

---

### Task 4: 今日报纸

**Files:**
- Create: `src/lib/desk-newspaper.ts`、`src/lib/desk-newspaper.test.ts`、`src/components/desk/newspaper.tsx`
- Modify: `src/routes/desk.tsx`（通栏挂报纸）

**Interfaces:**
- Consumes: `fetchSource`、`cookiesFromSettings`、`credentialTag`、`parseBoardDate`、`pixivRankingDateParam`、`rememberRanking`、`isBooru`、`siteLabel`、`BROWSE_STALE_MS`、`ProxiedImg`、`WorkCard`、`FetchOk`
- Produces: `newspaperItems(result: FetchOk | undefined): WorkCard[]`（仅 `pixivRanking` / `booruList`，`slice(0, 4)`）；`DeskNewspaper` 组件

queryKey 必须是 `["desk-newspaper", tab, safeMode, hideAi, credentialTag(cookie), dateIso]`。`enabled: hydrated && tab !== "fanbox"`。失败/空数组：组件 `return null`。成功且 `items.length > 0` 再 `rememberRanking`（Pixiv 在该函数里本就会 no-op，仍调用）。禁止 toast、禁止 `ArtworkCard`、禁止 `checkWatchArtists`。

- [ ] **Step 1: Write the failing test**

```ts
import assert from "node:assert/strict";
import { test } from "node:test";
import type { FetchOk, WorkCard } from "./types.ts";
import { newspaperItems } from "./desk-newspaper.ts";

function card(id: string): WorkCard {
  return {
    source: "yande",
    id,
    title: id,
    author: "a",
    authorId: "",
    thumb: `/t/${id}`,
    pageCount: 1,
    tags: [],
  };
}

test("空 / 错 op → []", () => {
  assert.deepEqual(newspaperItems(undefined), []);
  const related = { op: "pixivRelated", items: [card("1")] } as FetchOk;
  assert.deepEqual(newspaperItems(related), []);
});

test("pixivRanking / booruList 截到 4 张", () => {
  const items = [card("1"), card("2"), card("3"), card("4"), card("5")];
  assert.deepEqual(
    newspaperItems({ op: "pixivRanking", date: "20260916", items, nextPage: null }).map((x) => x.id),
    ["1", "2", "3", "4"],
  );
  assert.equal(newspaperItems({ op: "booruList", site: "yande", items: items.slice(0, 2), nextPage: null }).length, 2);
});
```

`FetchOk` 里 `booruList` 就是 `{ op, site: BooruSite, items, nextPage }`（`src/lib/types.ts`）。

- [ ] **Step 2: Run test to verify it fails**

Run: `node --experimental-strip-types --test src/lib/desk-newspaper.test.ts`

Expected: FAIL，模块不存在。

- [ ] **Step 3: newspaperItems + DeskNewspaper**

`src/lib/desk-newspaper.ts`：

```ts
/**
 * 案头报纸：从日榜响应抽出最多 4 张。
 *
 * 作用：DeskNewspaper 与单测共用截断，不把 UI 和 op 判别写两遍。
 */
import type { FetchOk, WorkCard } from "./types.ts";

export function newspaperItems(result: FetchOk | undefined): WorkCard[] {
  if (!result) return [];
  if (result.op === "pixivRanking" || result.op === "booruList") return result.items.slice(0, 4);
  return [];
}
```

`src/components/desk/newspaper.tsx`（`"use client"`）：

- `tab` / `safeMode` / `hideAi` / cookies 从 `useSettings` + `cookiesFromSettings`。
- `dateIso = parseBoardDate().iso`。
- `useQuery`：`staleTime: BROWSE_STALE_MS`。`queryFn`：
  - pixiv：`fetchSource({ data: { op: "pixivRanking", mode: "daily", page: 1, date: pixivRankingDateParam(dateIso), ...cookiesFromSettings() } })`
  - booru：`fetchSource({ data: { op: "booruList", site: tab, feed: "daily", page: 1, date: dateIso, ...cookiesFromSettings() } })`
  - 其它：throw 不应发生（`enabled` 已挡 FANBOX）。
- `isBooru(tab)` 才走 booru 分支。
- `items = newspaperItems(query.data)`。`useEffect`：`items.length > 0` 时 `void rememberRanking({ site: tab, period: "daily", date: dateIso, items })`。
- `tab === "fanbox"`：`return null`。
- `query.isPending`：通栏 + 4 个 `aspect-[3/4] rounded-lg bg-surface` 占位，栏名「今日报纸」。
- `query.isError || items.length === 0`（且非 pending）：`return null`。
- 成功：栏名是 `Link to="/rankings"`，文案 `今日报纸 · {siteLabel(tab)} 日榜`。下面 `flex gap-2 overflow-x-auto`（桌面可不滚）。每张：

```tsx
<Link
  to="/work/$source/$id"
  params={{ source: card.source, id: card.id }}
  className="relative block w-[4.5rem] shrink-0 overflow-hidden rounded-lg md:w-24"
>
  <ProxiedImg src={card.thumb} alt="" className="aspect-[3/4] w-full object-cover" />
  <span className="absolute left-1 top-1 text-[10px] tabular-nums text-accent-fg">{index + 1}</span>
</Link>
```

名次不要 `.kami-slip`。

- [ ] **Step 4: 挂到 DeskPage**

`DeskPage` 网格：

```tsx
<div className="grid grid-cols-1 gap-4 lg:grid-cols-3">
  <div className="lg:col-span-3">…页眉…</div>
  <div className="lg:col-span-3">{/* 去浏览暂仍通栏；Task 5 再按右栏是否空改 col-span */}</div>
  <div className="lg:col-span-3">
    <DeskNewspaper />
  </div>
</div>
```

本任务去浏览先 `lg:col-span-3`。Task 5 改成有右栏时 `lg:col-span-2`。

- [ ] **Step 5: Run tests**

```bash
node --experimental-strip-types --test src/lib/desk-newspaper.test.ts src/lib/desk-unread.test.ts
pnpm typecheck
```

Expected: 全 exit 0。

- [ ] **Step 6: Commit**

```bash
git add src/lib/desk-newspaper.ts src/lib/desk-newspaper.test.ts src/components/desk/newspaper.tsx src/routes/desk.tsx
git -c commit.gpgsign=false commit --no-gpg-sign -m "feat: 案头今日报纸（独立 desk-newspaper 查询）"
```

---

### Task 5: 去年今日笺、未拆信、未读纸叠、纸匣深链

**Files:**
- Create: `src/components/desk/letters.tsx`、`src/components/desk/stack.tsx`
- Modify: `src/routes/desk.tsx`、`src/routes/vault.tsx`

**Interfaces:**
- Consumes: `unreadItems`（Task 1）、`onThisDay`（`src/lib/storage/vault-profile.ts`）、`listVault`、`useViewHistory`、`useSettings.watchArtists`、`useWatchBadge`、`Link`
- Produces: `DeskLetters`、`DeskStack`；纸匣 `?recall=1` / `?unread=1` 初值

**未拆信文案（锁死）：**

- `watchArtists.length === 0` → 不渲染
- `newCount > 0` → `{n} 封 · {m} 张新作`（n = `watchArtists.length`，m = `useWatchBadge.getState().newCount`）
- `newCount === 0` → `信都拆过了`，`text-muted`
- 整块 `Link to="/watch"`。组件内不得 import `checkWatchArtists`。

**纸叠：** `unreadItems` 长度 0 不渲染；否则整块 `Link to="/vault?unread=1"`，文案 `未读 N 张`。视觉：外层 `relative h-24`，里面 3 个 `absolute inset-x-4 top-* bg-elevated rounded-lg`，`translate` 错开 4px（`translate-y-0 / 1 / 2` 或 `top-2/4/6`）。

**笺条：** `onThisDay(vault, Date.now())` 各组 length 之和为 N；N>0 才画 `.kami-slip`，文案 `去年的今天，你收了 N 张`，`Link to="/vault?recall=1"`。

**DeskPage 布局（锁死）：**

```
页眉通栏
笺条（有则，页眉下一行或右侧）
去浏览：lg:col-span-2；若信与纸叠都不存在 → lg:col-span-3
右栏：flex flex-col gap-4，只放入场的信/纸叠
报纸：lg:col-span-3
```

手机自然单列，DOM 顺序：页眉 → 笺 → 去浏览 → 报纸 → 信 → 纸叠。报纸在信前面（spec 手机顺序）。实现：网格里报纸仍在第三行通栏，右栏在第二列——**手机上 CSS grid 会变成：去浏览、右栏（信+叠）、报纸**，这与 spec「报纸在信前面」冲突。

要满足 spec 手机顺序，DOM 必须是：

```tsx
<header />
{slip}
<div className="grid grid-cols-1 gap-4 lg:grid-cols-3">
  <Link 去浏览 className={asideEmpty ? "lg:col-span-3" : "lg:col-span-2 lg:row-span-2"} />
  <DeskNewspaper className="lg:col-span-3 lg:col-start-1 lg:row-start-2" />
  <aside className="flex flex-col gap-4 lg:col-start-3 lg:row-start-1 lg:row-span-2">
    <DeskLetters />
    <DeskStack />
  </aside>
</div>
```

这样 DOM 顺序是 去浏览 → 报纸 → 信/叠（手机符合 spec）。桌面用 `row-start` 把右栏拉到第一行、报纸第二行通栏。`asideEmpty` 时不渲染 `<aside>`，去浏览 `lg:col-span-3`，报纸 `lg:col-span-3`。

案头拉纸匣：`useEffect` + `listVault()`，失败当 `[]`。历史：`useViewHistory((s) => s.items)`。未就绪时笺/叠不画（`vaultReady === false`）。

- [ ] **Step 1: 纸匣读 URL 初值**

`vault.tsx` 增加：

```ts
import { useSearchParams } from "next/navigation";
import { unreadItems } from "@/lib/desk-unread.ts";
import { useViewHistory } from "@/lib/view-history.ts";
```

（import 路径跟项目现有 `@/lib/...` 不带后缀的写法走，不要 `.ts` 后缀。）

```ts
const searchParams = useSearchParams();
const historyItems = useViewHistory((s) => s.items);
const [unreadOnly, setUnreadOnly] = useState(false);

useEffect(() => {
  const recall = searchParams.get("recall") === "1";
  const unread = searchParams.get("unread") === "1";
  if (!recall && !unread) return;
  if (recall) setRecallOnly(true);
  if (unread) setUnreadOnly(true);
  const url = new URL(window.location.href);
  url.searchParams.delete("recall");
  url.searchParams.delete("unread");
  window.history.replaceState(null, "", `${url.pathname}${url.search}${url.hash}`);
}, [searchParams]);
```

`unreadKeys`：`useMemo(() => new Set(unreadItems(all, historyItems).map((x) => x.key)), [all, historyItems])`。

`items` 在 `recallOnly` 过滤之后再：`unreadOnly && unreadKeys.size > 0 ? next.filter((item) => unreadKeys.has(item.key)) : next`。两 chip 同时亮 = 交集。

chip 区在「今日去年」旁增加：`unreadKeys.size > 0` 时渲染 `FilterChip`，文案 `unreadOnly ? "未读 ×" : "未读"`。`visibleCount` 的 reset effect 依赖数组加上 `unreadOnly`。

- [ ] **Step 2: Letters / Stack 组件**

`letters.tsx`：只读 store，无 fetch。结构：`Link to="/watch"` + `rounded-xl bg-surface p-5 shadow-[var(--shadow-paper-1)]` + 标题「未拆的信」+ 上面三元文案。

`stack.tsx` 接收 `count: number`，`count === 0` 时 return null（父级也可选择不挂）。`Link to="/vault?unread=1"`。

- [ ] **Step 3: 拼进 DeskPage**

按上面 DOM/grid。`asideEmpty = watchArtists.length === 0 && unreadCount === 0`（信在人数为 0 时不出现；纸叠在 count 0 不出现）。不要等报纸加载来决定 col-span。

- [ ] **Step 4: 断言案头源码不含检查函数**

```bash
node --experimental-strip-types --test src/lib/desk-unread.test.ts src/lib/desk-newspaper.test.ts
pnpm typecheck
```

另外用编辑器确认 `src/routes/desk.tsx` 与 `src/components/desk/**` **没有** `checkWatchArtists` 字符串。

Expected: 测试与 typecheck exit 0。

- [ ] **Step 5: Commit**

```bash
git add src/components/desk/letters.tsx src/components/desk/stack.tsx src/routes/desk.tsx src/routes/vault.tsx
git -c commit.gpgsign=false commit --no-gpg-sign -m "feat: 案头来信、未读纸叠、去年今日笺"
```

---

### Task 6: 收尾

**Files:**
- Modify: `CHANGELOG.md` Unreleased
- 不改 `docs/`（gitignore）

- [ ] **Step 1: CHANGELOG Unreleased「新增」三条量级的条目（用户口吻，不提路径实现细节以外的必要点）**

```md
## [Unreleased]

### 新增

- 今日案头：打开应用落到一张稿纸主页（今日日期、去浏览、当前站日榜四格、有追踪时的未拆信、有未读时的纸叠、去年今日笺）。浏览仍在，从案头「去浏览」或底栏「浏览」进入；详情「返回浏览」回到网格并保持滚动。
- 纸匣可从案头带上「未读」或「今日去年」过滤（可关闭的筛选片）；未读 = 近 90 天收进匣、收下后还没打开过的作品。
```

- [ ] **Step 2: 全量门禁**

```bash
pnpm typecheck
pnpm test
pnpm lint
pnpm build
```

每条必须 exit 0。`pnpm test` 会跑 `src/lib/**/*.test.ts`，含本刀三个测试文件。

- [ ] **Step 3: 若有 dev 服务，桌面 1280 与手机 390 各看一眼案头**

满态（有追踪、有未读）和瘦态（空匣、无追踪、FANBOX 无报纸）至少各一。390 无横向溢出；底栏第一格仍是「浏览」；人在案头时底栏无高亮。

- [ ] **Step 4: Commit + PR**

```bash
git add CHANGELOG.md src
git -c commit.gpgsign=false commit --no-gpg-sign -m "docs: CHANGELOG 今日案头"
```

分支 `feat/desk-home`，按项目惯例开 PR。CI 三 job 绿再合并。

---

## Self-Review

**Spec coverage**

| Spec | Task |
| --- | --- |
| `/` 案头、`/browse` 浏览、Logo/打开应用落案头 | 3 |
| 保活改挂 `/browse`；案头不卸保活；冷开不预挂浏览 | 3 |
| 桌面 NAV 案头第一；手机六格无案头 | 3 |
| `BackToBrowse` / 空态 / 404 / 标签搜索 → `/browse` | 3 |
| 站点切换留在案头或浏览，其它页去浏览 | 3 |
| e2e 改 `/browse` + 案头冒烟 | 3 |
| 去浏览纯字、折角、稿纸版式 | 3–5 |
| 报纸独立 query、4 张、FANBOX/失败隐藏、rememberRanking | 4 |
| 信不检查、文案锁死 | 5 |
| 未读 90 天窗纯函数 | 1 |
| 纸匣 `?unread=1` `?recall=1` + chip 交集 | 5 |
| 去年今日笺 `onThisDay` | 5 |
| 手机 DOM 顺序 去浏览→报纸→信/叠 | 5 |
| CHANGELOG / 门禁 | 6 |
| 不做：一纸两面、透台、来信改版、抽纸、checkWatchArtists | 全任务未引入 |

**Placeholder scan:** 无 TBD。`booruList` 测试对象已按 `FetchOk` 现字段写。

**Type consistency:** `unreadItems` / `workKey` / `newspaperItems` / `DeskPage` / `isBrowsePath` / `navItemActive` 前后任务同名。
