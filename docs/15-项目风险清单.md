# 15 — 项目风险清单（项目级）

> 与 14 技术债的区别：这里是**外部与组织性风险**（不是代码内部缺陷），按「概率 × 影响」排序。基线 `main@75e7de0`（2026-09-12 全量重推导）。

| 编号 | 风险 | 概率 | 影响 | 说明 | 缓解 |
| --- | --- | --- | --- | --- | --- |
| R-01 | **上游反爬升级导致功能面崩塌**：Pixiv ajax 接口改版 / Danbooru 收紧 Basic 免验证口径 / Cloudflare 策略变化 / konachan.net 镜像关闭 | 高（历史已多次：Cloudflare 403、镜像兜底、账号通道、日榜未公布窗口都是此类） | 高 | 全部核心功能依赖逆向的非官方接口；HTML 解析器（ascii2d、booru pool、SauceNAO 兜底）尤其脆弱 | 解析器单测固定样本快速定位破损点；上游抽象集中在 upstream/ 便于单点修复；upstream.md 沉淀逆向知识 |
| R-02 | **法律与合规**：聚合访问 + 绕过防护（Referer 伪造、UA 伪装 gallery-dl、镜像回退）+ FANBOX 付费内容下载 | 中 | 高（项目存续） | README 已自限「仅供学习交流、个人备份」；功能实质贴近平台 ToS 边界 | 保持个人使用定位、不开公开服务、不分发抓取数据。**2026-09-11 拍板：不加「付费通道默认关闭」选项**，维持 README 自限为主要缓解 |
| R-03 | **单维护者 + AI 协作生成**：知识集中在 CHANGELOG、本套文档与智能体约定 | 高 | 中 | 巴士因子=1 | 本套文档 + 09 开发指南 + CI 门禁（TD-03）降低对人的依赖 |
| R-04 | **数据丢失窗口**：PGlite 快照 0.5-30s 窗口；user_sync LWW 覆盖（时钟偏移） | 中 | 中 | 强杀进程/并发设备可丢最近写入 | TD-08/09/19/22 已修（原子写、分段 LWW、全表快照、失败行隔离）；auth/sync 写后补偿 dump；用户侧定期备份导出 |
| R-05 | **凭据泄露连锁**：localStorage 明文（SEC-03 残余）+ legacy 明文落盘（SEC-14）+ LAN 暴露面 | 中（依赖网络环境） | 高（图站账号被盗用） | 家庭网络被入侵 / 公共网络误跑 / .data 目录或备份外发 | SEC-01/02/04/05/07/09/10/11/12 已修；**待办 SEC-14 关 legacy 明文**；文档警示勿在不可信网络运行 |
| R-06 | **平台依赖残留**：Grok App Builder 工具链半死代码（startup.sh 引已删脚本、.grok/、.vercel/、.tmp-kami-root.txt 误入库） | 中 | 低 | 继续演化会积累「看似存在实则无效」的保障 | TD-33/34/35 工程卫生批一次清掉 |
| R-07 | **依赖供应链**：29 个运行时依赖中 better-auth/pglite/puppeteer-core/undici/next 均快速演进 | 中 | 中 | 破坏性升级直接影响出站/认证核心 | pnpm-lock 提交、audit 清零、大版本升级走 PR + CI 五门禁 + 冒烟（#112 先例） |
| R-08 | **磁盘增长失控**：media/source 已有水位，但 **vault 与 rankings.sqlite 不参与治理（TD-31）** | 高（长跑） | 低-中 | .data/vault 与榜单快照只增不减 | TD-31（rankings 保留期上限 + vault 容量视图）；运维手册列手动清理路径 |
| R-09 | **浏览器 API 依赖**：FS Access API / IndexedDB 句柄在非 Chromium 浏览器退化 | 低 | 低 | Firefox/Safari 纸匣镜像降级（代码已有降级路径） | 维持降级；文档标注最佳浏览器 |
| R-10（新） | **CI e2e 打真实上游**（yande）：上游抖动/风控时 CI 红 | 中 | 低 | e2e job 依赖外部可用性 | retries=1 + 关键路径软通过 + annotation；上游解析已有固定样本单测兜底 |
| R-11（新） | **前沿/实验性技术栈组合**：node:sqlite DatabaseSync（实验性）、tsgo 原生预览编译器（7.0-dev）、PGlite WASM（强杀损坏→已用纯内存+快照规避）、Next 16.3 / React 19 / better-auth 1.7 新大版本 | 中 | 中 | 任一上游 breaking 直接打在核心链路；Node 小版本升级可能动 node:sqlite | 锁版本（engines ≥22 + lockfile）；CI 全门禁；升级一律 PR + 全量测试 |
| R-12（新） | **多进程/多实例快照互踩**：PGlite 内存库 + 快照 watch 是进程内的，多 worker 各自 restore/dump 同一 .data/kami-db-snapshot.json 相互覆盖【需要确认：当前所有部署形态是否恒为单进程】 | 低 | 中 | 未来若开集群/多副本，账号数据静默互踩 | 部署文档注明单实例约束（08 号）；多实例需求出现时先做外部锁或迁常驻 PG |

## 状态更新（2026-09-12，基线 `main@75e7de0`）

- R-03 ✅ CI 门禁（含 docker/e2e）+ 文档体系持续同步
- R-04 ✅ 基本闭合：TD-08/09/19 已修；TD-22 快照失败行隔离于 bd1f28c 落地——强杀窗口从「可能丢行」收敛为「最差 30s 内变更丢失」
- R-05 大部闭合：SEC-01/02/04/05/07/09/10/11/12 已修（bd1f28c 补齐 auth 限速与 sync 同站校验）；**残余 = localStorage 明文（长期）+ SEC-14 legacy 明文落盘（待修，小改）**
- R-06 残留待清：.tmp-kami-root.txt（TD-33）、startup.sh（TD-34）、.grok/.vercel 目录
- R-07 ✅ 锁文件 + CI；#112 大版本升级先例验证流程
- R-08 部分：media/source 水位 ✅；vault/rankings 无界 = TD-31
- R-01 持续（不可控，缓解有效）

## 风险总评

项目最大的存在性风险仍是 **R-01（上游对抗）** 与 **R-02（合规）**——它们决定这个产品「还能不能跑」，且都不完全可控。代码层面：三批安全/工程修复 + bd1f28c 认证专项后，存量风险已从「凭据裸奔/数据丢失」收敛为**散点式小缺陷**（TD-23~40：P2 × 4、P3 × 14，无 P0/P1）与**一个待修安全项 SEC-14**；新增的 R-10~12 属可控的工程化风险。上一版「当前技术债务清单无活跃项」的表述已失效（2026-09-12 重推导新发现 18 项，均为小改）。
