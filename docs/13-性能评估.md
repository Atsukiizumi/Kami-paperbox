# 13 — 性能评估

> 基线 `main@75e7de0`（2026-09-12 全量重推导）。

## 13.1 已落地的性能工程（值得记录的决策）

| 优化 | 实现 | 效果依据 |
| --- | --- | --- |
| undici 持久连接池替代每请求 curl 进程 | curl-fetch.server.ts:189-205（connect 15s/headers 45s/16 连接）+ 3 败 10min 熔断回 curl | CHANGELOG：Pixiv 单请求 1-6s → 预热 0.2-0.4s；详情页 5.7s → 毫秒级 |
| 三层读缓存 | localStorage browse-cache(24h) → React Query(30min) → unstable_cache(1800s) + .data/source(30min) + .data/media(7d) | 首屏先画本地、30min 内零上游 |
| 首屏同步 hydrate | QueryClient 创建时 hydrateBrowseCache（providers.tsx） | 无 loading 白屏闪烁 |
| 瀑布流一次成版 | packJustified + CSS 变量 + 首排禁 transition；骨架同算法 | 真图进来不跳版 |
| 媒体并发闸 + 重试 + 单飞 | media-gate（默认 6）+ 429/503 退避 + inflightMedia 去重 | 防上游限流雪崩 |
| 浏览器加载车道 | media-lane 4 条 + priority 插队 | 点击→跳转 10.7s/1.1s → 0.2-0.9s |
| 详情查询键统一 | work-detail.ts 共用键+拉取 | 悬停 30s 内点开命中缓存 |
| 感知优化 | 悬停 160ms prefetch、封面懒加载 800px、ugoira 进视口才拉、悬停预览升清 | — |
| 队列进度节流 + 分批渲染 | 200ms 节流同值去重；纸匣首批 60 续 40 | PER-2/3 ✅ |
| 浏览器端 GIF 编码让步 | gifenc 每帧 setTimeout(0) | 不卡主线程 |

## 13.2 当前风险与潜在瓶颈

> PER-1~5 ✅ 已修（水位/节流/分批/单飞/流式断流）；PER-6/7 维持。**本期新增 PER-8~14（2026-09-12 重推导）。**

| 编号 | 风险 | 位置 | 说明 | 建议 | 需验证 |
| --- | --- | --- | --- | --- | --- |
| PER-6 | PGlite 单连接排队 + 全表 JSON 快照 | pglite-dialect.ts:47-70 | 单用户量级无碍 | 维持；账号量级增长再议增量 | 否 |
| PER-7 | 登录中转 280ms JPEG 帧轮询 | session-relay.tsx | LAN 可接受；访客 401 空转已修（bd1f28c：非 2xx 快速失败），帧通道结论不变 | 维持 | 否 |
| **PER-8** | 回源上限 48MB 但落盘上限 8MB：大图（原图/大 ugoira zip）**永不缓存**，每次全额回源 | media-cache.server.ts:16,104 vs media.ts:42 | 与「一周内不再打源站」的缓存目标在大文件场景不符 | 落盘上限与回源上限解耦（可配）或大文件单独低频缓存区 | 是（统计大图占比） |
| **PER-9** | pixivUser 每次翻页重拉全量 `profile/all`（可达数万 id）+ 60 个批量 illusts，且该 op 不进 source-cache | pixiv.ts:412-419、source-cache.server.ts:69-70 | 大画师翻页放大明显 | pixivUser 列表页进缓存（短 TTL）或改游标 | 是（大画师实测量） |
| **PER-10** | danbooru JSON 与图片全走 curl 子进程通道（绕过 undici 池） | booru-sites.ts:52-64、media.ts:181-200 | 一页 40 条反复付出进程/TLS 成本 | 池通道带 Basic UA 试跑 danbooru，挑战页才降级 curl | 是（A/B 计时） |
| **PER-11** | sweepCache 异步壳内全同步 IO（readdir/stat/unlink Sync） | cache-watermark.server.ts:64-107 | 数万缓存文件时事件循环阻塞尖峰 | 每批 yield（setImmediate）或 worker 化 | 是（造 2 万文件计时） |
| **PER-12** | 榜单未公布窗口最坏 3 连回源（原始→昨天→无日期），翻页每页可能重复 | pixiv.ts:136-149 | 放大对 pixiv 的请求量 | 回落结果按日期进程内短记忆 | 否 |
| **PER-13** | AppShell（providers 层全路由常驻）直接 import 首页 778 行组件 | app-shell.tsx:20 | 任何页面都背首页 bundle | next/dynamic 拆首页 | 是（bundle 分析） |
| **PER-14** | hydrate 后对带 queryFn 的键立即补刷，秒切 tab 时三路 home-* 并发补刷无上限 | browse-cache.ts:114-133 | 与 media-lane 只管图片不同 | 补刷也进车道或当前 tab 优先 | 否 |

**不存在的问题**（澄清维持）：无 ORM 关系查询故无 N+1；SQL 均走索引；列表分页在源站侧完成。

## 13.3 是否需要 Benchmark/Profiler

PER-8/9/10/11/13 建议先实证再动手（上表已标）；渲染类问题一律先 React Profiler 定位（PER-2 的既有教训）；磁盘类先采样增速再定阈值。
