# 14 — 技术债务清单

> 编号 TD；优先级 P0（立即）/P1（高）/P2（中）/P3（长期）。安全类债务在 12 文档有独立编号，此处只引用。
>
> **状态（2026-09-12 复核，基线 `main@1e96f5d`）**：TD-00 ~ TD-19 共 20 项**全部闭环**（09-11 复核 + 同日清偿，逐项与代码核对：拆分文件、CI、水位、原子写、分段表 migration 等产物俱在；TD-19/14/15 于同日清偿，见各行）。TD-20 ~ TD-22 为 09-11/12 认证专项审查与文档复审**新增待清**（见各行）；安全类新发现另入 12 号文档 SEC-11 ~ SEC-13。大版本依赖升级（Next 16.3 / TS 6+tsgo / better-auth 1.7）已随 PR #112 落地，见 16 路线。

| 编号 | 问题 | 代码位置 | 根本原因 | 风险 | 影响范围 | 修改复杂度 | 推荐方案 | 优先级 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ~~TD-00~~ | ~~数据面 API 无鉴权（=SEC-01/02/07）~~ | ✅ 已修（90a9050） | — | — | — | — | withDataPlane 全量接线 + LAN 启动令牌 | ✅ |
| ~~TD-01~~ | ~~upstream.server.ts 上帝模块（1257 行）~~ | ✅ 已修（b8d3c0e） | — | — | — | — | 拆 `src/lib/upstream/` 六模块 + 桶导出 + 7 例固定样本映射测试 | ✅ |
| ~~TD-02~~ | ~~测试清单手工漂移：10 个测试文件从未运行~~ | ✅ 已修（90a9050） | — | — | — | — | package.json test 脚本改 glob（`scripts/**/*.test.mjs` + `src/lib/**/*.test.ts`） | ✅ |
| ~~TD-03~~ | ~~无 CI~~ | ✅ 已修（90a9050） | — | — | — | — | .github/workflows/ci.yml：typecheck+test+lint+build 四门禁 | ✅ |
| ~~TD-04~~ | ~~三代框架死代码并存~~ | ✅ 已修（defa96f 等） | — | — | — | — | Grok 平台完全退出：server/、__grok/、multiplayer/、app-data/、pnpm-workspace、OAuth/Gate 全删；残留 .grok/、startup.sh、.vercel/ | ✅ |
| ~~TD-05~~ | ~~tsconfig 不含 scripts~~ | ✅ 已修 | — | — | — | — | include 覆盖 scripts | ✅ |
| ~~TD-06~~ | ~~双锁文件矛盾~~ | ✅ 已修 | — | — | — | — | pnpm-lock 提交，Dockerfile corepack | ✅ |
| ~~TD-07~~ | ~~缓存目录无容量治理（=PER-1）~~ | ✅ 已修 | — | — | — | — | cache-watermark.server.ts：50 次写惰性扫描，media 2GB / source 512MB 可配 | ✅ |
| ~~TD-08~~ | ~~vault put 先删后写非事务~~ | ✅ 已修 | — | — | — | — | 暂存 + 事务先行 + rename 原子替换（vault-store.server.ts:210-298） | ✅ |
| ~~TD-09~~ | ~~user_sync 整载荷 LWW + payload 无校验（=DQ-2/DQ-3/SEC-09）~~ | ✅ 已修（df326cf） | — | — | — | — | user_sync_segments 四段 LWW + validateSegment 分段校验 + 旧单行兼容读取（过渡期） | ✅ |
| ~~TD-10~~ | ~~明文凭据多落点（=SEC-03/04）~~ | ✅ 已修（第三批，残余见下） | — | — | — | — | 备份 v2 口令加密 + 同步设置段 KEK 密文 + queryKey 指纹；**残余**：localStorage kami-settings 明文（长期方案，见 12 文档） | ✅（残余） |
| ~~TD-11~~ | ~~Konachan 镜像兜底 200+HTML 盲区~~ | ✅ 已修（b8d3c0e） | — | — | — | — | 200+HTML 也切镜像 | ✅ |
| ~~TD-12~~ | ~~翻页「还有下一页」启发式脆弱~~ | ✅ 已修（15f8ed7） | — | — | — | — | booruList / pixivSearch 用过滤前原始条数判断 | ✅ |
| ~~TD-13~~ | ~~配置漂移：tineye 键静默忽略；DANBOORU_UA 绑定~~ | ✅ 已修（15f8ed7） | — | — | — | — | tineye 残留核实已不存在；DANBOORU_UA 抽常量并注明维护策略（booru.ts:53） | ✅ |
| ~~TD-14~~ | ~~过时注释与死引用存量~~ | ✅ 已修（2026-09-11） | — | — | — | — | reverse-engineering.md 尾注改为指向方法 B 拦包输出；08 手册删 sniff-xhr 死链行 | ✅ |
| ~~TD-15~~ | ~~未使用依赖~~ | ✅ 已修（2026-09-11） | — | — | — | — | 移除 cmdk / vaul / react-resizable-panels / jose 四个零引用依赖；jose 仍以 better-auth 传递依赖存在（其 dependencies 直接声明），构建不受影响 | ✅ |
| ~~TD-16~~ | ~~vault/rankings 页异步无竞态防护~~ | ✅ 已修（15f8ed7） | — | — | — | — | 请求令牌防竞态（vault.tsx:79、rankings.tsx） | ✅ |
| ~~TD-17~~ | ~~Docker 烧死 VITE_AUTH_ENABLED=false~~ | ✅ 已修（8fa2245） | — | — | — | — | compose 默认 `"true"`，ARG 可覆盖；README 对齐 | ✅ |
| ~~TD-18~~ | ~~backup/restore 对 user_sync 单行整覆写~~ | ✅ 并入 TD-09 | — | — | — | — | 分段化后旧单行仅作过渡期读取 | ✅ |
| ~~TD-19~~ | ~~快照表清单滞后：TABLES 硬编码四表，不含 0003 新增的 user_sync_segments / user_sync_meta~~ | ✅ 已修（2026-09-11） | — | — | — | — | 清单模式改为「public 下除 _migrations 外全量动态枚举」（db-snapshot.server.ts snapshotTables），新表自动纳入；restore 按 FK 依赖拓扑排序（account/session 引用 user，字母序会先插子表被 FK 静默吞行——复审发现同日修复）；新增 db-snapshot.server.test.ts 三条回归（分段数据写入→导出→新库恢复不丢；FK 子表 user/session/account 全量恢复；新建表自动进快照且 _migrations 排除）；db.ts 在 node:test 下跳过真实 .data 引导 | ✅ |
| TD-20 | 访客层绑定链路 401 盲区：`/api/login-browser` 未开 guest，session-relay 把 401 JSON 当 JobView 解析（无 status 字段）→ 280ms 无限轮询、登录对话框卡死（Onboarding 对访客可达）；`/api/sessions`、`/api/whoami` 未开 guest → 访客 HttpOnly 副本建不成，/api/media 读不到 kami_* cookie → FANBOX/R-18 图挂、whoami 头像不显示；前端对 401 全静默、无「请登录」引导 | session-relay.tsx:83-114；app/api/{login-browser,sessions,whoami}/route.ts；apply-session.ts:29（吞 401）；store.ts:297；account-sync-bridge.tsx:69 | 访客层（9171bf3）只放行了内容面路由，绑定链路三条支撑路由未随行调整；authEnabled 形态无全局 401 拦截（LanTokenGate 仅账号关闭形态挂载） | 访客无法走浏览器登录 Pixiv（只能手贴 Cookie）、FANBOX/R-18 图挂、个人面操作无声失败——「凭据只存浏览器」的访客承诺缺了绑定通道 | 登录/设置页 + 访客全部内容面 | 中 | login-browser / sessions / whoami 按内容面口径评估开 guest（注意 sessions 放行即恢复访客 HttpOnly 副本，需一并评估 media 效果），或 UI 层对未登录禁用入口并提示；session-relay 对非 2xx 立即报错退出；可选全局 401 提示 | P1 |
| TD-21 | 账号同步 KEK 密文拉取整段替换，可覆盖访客期新录凭据——「凭据永不因同步而丢」不变量（account-sync.ts:10-14）在「有 KEK + 远端 cipher 段」路径不成立 | account-sync.ts:218-219 → backup-client.ts:107-115（applySegment 整段 setState） | 三道守卫覆盖「无 KEK 不推」「密文无 KEK 跳过拉取」「plain credsOmitted 保留本地」，唯独有 KEK 的密文拉取走整段替换；访客期本地改动没有 exportedAt，marker 比较必输 | 登出（访客）期间换绑的 Pixiv cookie 被远端旧档覆盖，且随后作为「新改动」推回服务端、回滚被固化 | 多设备 / 登出期间改过凭据的用户 | 中 | 密文解开后对凭据子字段（pixivCookie 等，与 omitSettingsCredentials 同清单）做与 plain 路径一致的本地保留合并；或为凭据子字段单独记脏标记 | P2 |
| TD-22 | 快照 restore 部分失败仍立即回写，固化丢行（TD-19 同族） | db-snapshot.server.ts:125-127（单行失败 warn 跳过）、:184（恢复完无条件 dumpNow） | restore 失败行不会出现在后续 capture 结果里，恢复完成即落盘 → 快照被覆盖 | dev 启动期恢复部分失败时该行（可能是 session/账号行）永久丢失；8fb71ce 只堵了 build 期触发，此路仍在 | 低-中（个人机低频，但后果是凭据级） | 低 | restore 返回失败行计数，非零时跳过首次 dumpNow（留待 30s 看护周期核对）或把失败行保留在快照；补一条「恢复失败不回写」回归测试 | P2 |

**量化概览（2026-09-12）**：TD-00 ~ TD-19 共 20 项全部闭环；TD-20 ~ TD-22 新增待清（P1 × 1、P2 × 2）。安全类新发现见 12 号文档 SEC-11 ~ SEC-13。
