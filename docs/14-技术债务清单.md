# 14 — 技术债务清单

> 编号 TD；优先级 P0（立即）/P1（高）/P2（中）/P3（长期）。安全类债务在 12 文档有独立编号，此处只引用。
>
> **状态（2026-09-10 第一批修复后）**：TD-00 ✅（含 SEC-01/02/07/10，见 12 文档）、TD-02 ✅（glob 自动发现，243 测试全量跑；`scripts/` 只保留活基础设施两个测试文件，死工具链的 grok-pwa-plugin / check-auth-invariant 测试待 TD-04 一并删）、TD-03 ✅（`.github/workflows/ci.yml`：typecheck+test+lint+build，Node 22）。另顺手修掉 4 个存量 lint error 与 ranking-store 的 Windows sqlite 句柄问题。

| 编号 | 问题 | 代码位置 | 根本原因 | 风险 | 影响范围 | 修改复杂度 | 推荐方案 | 优先级 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TD-00 | 数据面 API 无鉴权（=SEC-01/02/07） | src/routes/api/** | runAuth 未接线 | 凭据/数据泄露 | 全部端点 | 低（现成函数） | 统一包 runAuth + 关闭态令牌 | **P0** |
| TD-01 | upstream.server.ts 上帝模块（1257 行） | src/lib/upstream.server.ts | 五站点映射长进一个文件 | 每次站点变更都碰核心文件；回归面大 | 上游层 | 中 | 拆 `adapters/<site>/*.ts` + 共享 media 代理独立文件；保持 dispatch 签名不变使上层零改动 | P1 |
| TD-02 | 测试清单手工漂移：10 个测试文件从未运行 | package.json:24 | 显式清单 vs glob | 假绿；关键模块（view-history/ranking-store 等）无回归保护 | 测试体系 | 低 | lib 段改 glob 自动发现 | **P0** |
| TD-03 | 无 CI（.github 只有模板） | .github/ | Grok 沙箱时代无此需求 | 质量门禁靠人肉 | 全仓库 | 低 | GitHub Actions：typecheck+test+lint+build | **P0** |
| TD-04 | 三代框架死代码并存 | server/middleware/、scripts/grok-pwa-plugin.mjs、app-env-plugin.mjs、swallow-abort-plugin.mjs、src/lib/multiplayer/、.vercel/output、.output、pnpm-workspace nf3 override | TanStack/Nitro→Next 迁移未清尾 | 误导维护者；部分工具（check:auth、preview.mjs、manifest 链接）已失效仍被引用 | 工程认知成本 | 低-中 | 一次性清理 PR：删死代码；修 layout.tsx manifest 死链（删引用或补静态 manifest）；确认 brand-check 依赖后处理 grok-pwa-shared | P1 |
| TD-05 | tsconfig 不含 scripts/server | tsconfig.json:39-45 | include 白名单 | 工程脚本与中间件零类型检查 | scripts/server | 低 | include 加 scripts（allowJs 已开）；server/ 若保留则一并 | P1 |
| TD-06 | 双锁文件矛盾：pnpm-lock 被 gitignore + Dockerfile 用 package-lock（npm ci） | .gitignore:39、Dockerfile:11-12 | 两代包管理并存 | 本机/Docker 依赖树不一致，构建不可复现 | 部署 | 低 | 二选一（建议：提交 pnpm-lock，Dockerfile 改 corepack+pnpm） | P1 |
| TD-07 | 缓存目录无容量治理（=PER-1） | media-cache/source-cache | 惰性删除设计 | 磁盘无界 | .data | 低 | 启动清理 + 水位 | P1 |
| TD-08 | vault put 先删后写非事务；用户文件夹写非原子 | vault-store.server.ts:189-199、folder-access.ts:133-139 | 无 tmp+rename | 崩溃留孤儿/半文件 | 纸匣完整性 | 中 | tmp 写成功后 rename（db-snapshot 已有范本） | P1 |
| TD-09 | user_sync 整载荷 LWW 无合并（=DQ-2）+ payload 无校验（=SEC-09） | account/sync/route.ts | 快速实现 | 多设备互相覆盖 | 同步 | 中 | 分段 LWW + zod 校验 | P2 |
| TD-10 | 明文凭据多落点（=SEC-03/04） | store/accounts/backup/browse-cache | HttpOnly 通道未成主通道 | 窃取面大 | 账号体系 | 高（兼容迁移） | 见 12 文档方案 | P1 |
| TD-11 | Konachan 镜像兜底存在 200+HTML 盲区 | upstream.server.ts:901 | 只判状态码 | 挑战页期功能假死 | konachan | 低 | 补 text.startsWith("<") 时也重试镜像 | P2 |
| TD-12 | 翻页「还有下一页」启发式脆弱（>=20 / >=8） | upstream.server.ts:357-361、952-958 | 以返回条数推断 | 过滤后不足阈值被误判末页 | 搜索/列表翻页 | 低 | 用源站 total/最后一页标记（booru 有 count 可用） | P2 |
| TD-13 | 配置漂移：tineye 键被静默忽略；DANBOORU_UA 伪装 gallery-dl 版本绑定 | kami.config.json、booru.ts:49 | 引擎下线/UA 策略残留 | 误解配置生效 | 配置 | 低 | 删 tineye；UA 抽常量并注明维护策略 | P3 |
| TD-14 | 过时注释与死代码存量 | ranking-archive.ts:2、UC:707-709/1020-1022、user.$id.tsx:80、store.ts:354-357 等 | 快速迭代未清 | 认知误导 | 可读性 | 低 | 顺手清（每次触碰文件时） | P3 |
| TD-15 | 未使用依赖（recharts 已核实；react-day-picker/date-fns【基于源码推导】模板连带） | package.json:70-73 | shadcn 全量安装 | 包体/安装面 | 依赖 | 低 | 逐个 grep 后移除 | P3 |
| TD-16 | vault/rankings 页异步无竞态防护 | vault.tsx:52-76、rankings.tsx:65-80 | 无 AbortController | 快速切换时旧响应覆盖新 | 两个页面 | 低 | 请求 token 或 AbortController | P3 |
| TD-17 | Docker 镜像烧死 VITE_AUTH_ENABLED=false，功能矩阵与 README 未对齐 | Dockerfile:16,36 | 构建参数硬编码 | Docker 用户无应用账号/同步 | Docker 形态 | 低 | 改 build ARG 默认 true；README 补差异表 | P2 |
| TD-18 | backup/restore 对 user_sync 单行整覆写无版本 | backup.ts | 与 TD-09 同源 | 同上 | 同步 | — | 并入 TD-09 | P2 |

**量化概览**：P0 × 3 已全部清零（TD-00/02/03，2026-09-10）。下一批重心：P1 × 7（TD-01 拆 upstream 因 R-01 上提、TD-04/05/06/07/08、TD-10 凭据收敛）——约三分之二集中在「迁移清尾」「凭据收敛」「工程门禁」三件事上，详见 16 路线。
