# 14 — 技术债务清单

> 编号 TD；优先级 P0（立即）/P1（高）/P2（中）/P3（长期）。安全类债务在 12 号文档有独立编号，此处只引用。
>
> **状态（2026-09-12 全量重推导，基线 `main@75e7de0`）**：TD-00~19 历史项全部闭环（压缩保留供追溯）；**TD-20~22 已于同日 bd1f28c 清偿**（含配套测试）；**TD-23/24/25/38 已于同日修复（S10~S11 批次，PR #115）**；**TD-26/33/34/35 已修（S12~S13 批次，PR #117）**；**TD-27/28/29/36/39 已修（M8 批次，PR #119）**；其余 TD-30/31/32/37/40 待清。无 P0、无 P1。

| 编号 | 问题 | 代码位置 | 根本原因 | 风险 | 影响范围 | 修改复杂度 | 推荐方案 | 优先级 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ~~TD-00~~ | ~~数据面 API 无鉴权（=SEC-01/02/07）~~ | ✅ 已修 | — | — | — | — | withDataPlane 全量接线 + LAN 令牌 | ✅ |
| ~~TD-01~~ | ~~upstream.server.ts 上帝模块（1257 行）~~ | ✅ 已修 | — | — | — | — | 拆 upstream/ 六模块 + 桶导出 + 固定样本测试 | ✅ |
| ~~TD-02/03/04/05/06~~ | ~~测试清单漂移 / 无 CI / 三代框架死代码 / tsconfig / 双锁文件~~ | ✅ 已修 | — | — | — | — | glob 测试 / ci.yml / Grok 平台退出等 | ✅ |
| ~~TD-07~~ | ~~缓存目录无容量治理（=PER-1）~~ | ✅ 已修 | — | — | — | — | cache-watermark：50 写一扫，media 2GB / source 512MB 可配 | ✅ |
| ~~TD-08~~ | ~~vault put 先删后写非事务~~ | ✅ 已修 | — | — | — | — | tmp 暂存 + BEGIN IMMEDIATE + rename 原子替换 | ✅ |
| ~~TD-09/18~~ | ~~user_sync 整载荷 LWW + 无校验~~ | ✅ 已修 | — | — | — | — | 四段 LWW + validateSegment + 旧单行过渡读取 | ✅ |
| ~~TD-10~~ | ~~明文凭据多落点（=SEC-03/04）~~ | ✅ 已修（残余 localStorage 明文属长期方案） | — | — | — | — | 备份 v2 + 同步 KEK 密文 + queryKey 指纹 | ✅（残余） |
| ~~TD-11/12/13/14/15/16/17~~ | ~~镜像盲区 / 翻页启发式 / 配置漂移 / 死注释 / 未用依赖 / 页竞态 / Docker 默认~~ | ✅ 已修 | — | — | — | — | 见历史版本与 CHANGELOG | ✅ |
| ~~TD-19~~ | ~~快照表清单滞后~~ | ✅ 已修 | — | — | — | — | 全表动态枚举 + FK 拓扑恢复 + 三条回归 | ✅ |
| ~~TD-20~~ | ~~访客层绑定链路 401 盲区~~ | ✅ **bd1f28c** | — | — | — | — | login-browser / sessions / whoami 开 guest（绑定链路属内容面：凭据不落服务端、镜像 per-browser、whoami 空 cookie 早退）；session-relay 非 2xx 快速失败；e2e 补访客 sessions 200 | ✅ |
| ~~TD-21~~ | ~~KEK 密文拉取整段替换覆盖访客期凭据~~ | ✅ **bd1f28c** | — | — | — | — | SENSITIVE_SETTINGS_FIELDS 共用清单 + 脏标记 `kami-cred-dirty-v1` + mergeCredentialsOnPull 本地非空保留 + 桥接合并后补推 settings + 推送成功清脏；单测 5 例锁字段集 | ✅ |
| ~~TD-22~~ | ~~快照 restore 部分失败仍回写固化丢行~~ | ✅ **bd1f28c** | — | — | — | — | 失败行尾部重试一轮 → 仍失败隔离落盘 `.data/kami-db-snapshot.restore-failed.json`（tmp+rename），主快照照常演进——失败行永不静默销毁；回归测试 | ✅ |
| ~~**TD-23**~~（✅ S10~S11 批次修复） | user 页关注乐观更新 setQueryData 的键用 **cookie 原文**，与 queryKey 的 `credentialTag(pixivCookie)` 指纹不一致 → 写入无人读取的死键，关注/取关按钮不即时变化（需重新进入页面才刷新）。**已人工核验**：src/routes/user.$id.tsx:28 vs :109 | user.$id.tsx:28,109 | queryKey 纪律（SEC-04 指纹化）改造时漏改了本页的 setQueryData | 功能 bug（可见） | 画师页关注交互 | 极低 | setQueryData 改用同一 `credentialTag(pixivCookie)`；顺带全仓核对 setQueryData 与 useQuery 键一致性 | P2 |
| ~~**TD-24**~~（✅ S10~S11 批次修复，单测锁） | danbooru safeMode 失效：未登录 2 标签上限下用户输满 2 标签时 `rating:g` 被「挤掉」不再附加（moebooru 站 rating:s 无条件附加，行为不对称） | booru.ts:123-127 | 「用户词优先」取舍与 safeMode 全局语义冲突 | safeMode 下 danbooru 搜索仍出成人内容 | safeMode 用户 × danbooru | 低 | safeMode 时 rating:g 强制附加并放行第 3 标签，或客户端拦截提示 | P2 |
| ~~**TD-25**~~（✅ S10~S11 批次修复：legacy 写路径 410 关闭） | 过渡期 legacy 整份同步接受明文 settings 写 user_sync.payload，随快照全表导出明文落盘 | account/sync/route.ts:146-156 | 分段化上线后 legacy 写路径未关 | 磁盘泄露=会话明文 | 残留 legacy 客户端路径 | 低 | 关闭 legacy 写（410）或强制 omitSettingsCredentials | P2 |
| ~~**TD-26**~~（✅ PR #117：e2e 专用 8090 端口，复用只可能命中隔离 server） | e2e `reuseExistingServer: !CI`：本地 8080 已有普通 dev server 时直接复用，e2e 账号/快照写进真实 `.data`（隔离只在 e2e-server 自起时成立） | playwright.config.ts:28 | 复用语义与数据隔离假设冲突 | 测试数据污染真实库 | 本地跑 e2e 的开发流程 | 低 | 本地也强制独立 server，或启动前断言 KAMI_ROOT 指向临时根 | P2 |
| ~~**TD-27**~~（✅ PR #119） | 服务端双层缓存 TTL 分散两处（Next unstable_cache 1800 与 SOURCE_CACHE_TTL_MS 30min 恰好相等才一致）；且 unstable_cache 命中不触发 noteCacheWrite，水位扫描频率低于真实写入 | routes/api/source.ts:22-31、source-cache.server.ts:17 | 两层缓存各自记账 | 改任一侧产生「已过期数据仍回」漂移；磁盘水位低估 | 列表缓存调参与容量治理 | 低 | TTL 抽单一常量两处引用；水位计数挪到外层 | P3 |
| ~~**TD-28**~~（✅ PR #119） | 上游 JSON 请求不传 AbortSignal、无总超时（无代理直连时 Node fetch 默认无超时；代理路径有 undici 45s 兜底） | upstream/http.ts:39、booru-sites.ts:66 | 超时策略只做在出站层一半 | 上游挂起占住请求与连接 | 全部列表/详情请求 | 低 | outboundFetch 统一 AbortSignal.timeout(45s) | P3 |
| ~~**TD-29**~~（✅ PR #119） | save-work fetchBlob 写死 90s 超时、无重试、无 429 处理，与媒体代理层行为不一致；多页作品中途失败整单报错重头 | save-work.ts:14 | 收藏管线自建取数绕过媒体层 | 大作品收藏成功率低 | 下载/收藏大作品 | 低 | 复用 /api/media 通道或补退避重试 | P3 |
| **TD-30**（新） | exported_at 类型不一致：0002 user_sync 为 bigint，0003 user_sync_segments 为 text（毫秒字符串） | migrations/0002:5、0003:8；route.ts 写 String/读 Number | 两代迁移约定漂移 | 未来 SQL 级 order by/比较在 text 列按字典序出错 | 同步数据查询演进 | 低 | 新迁移统一为 bigint（或 timestamptz），读侧兼容 | P3 |
| **TD-31**（新） | rankings.sqlite 与 .data/vault 不参与水位治理：榜单「不设条数上限」、vault 无界 | cache-watermark.server.ts:64-65、ranking-store.server.ts:6 | 水位只覆盖缓存目录 | 长跑实例磁盘无界（TD-07 只修了一半面） | 长期运行实例 | 低 | rankings 加保留期数上限；vault 提供容量视图 | P3 |
| **TD-32**（新） | asRecord/asString/asNumber/asBool 四件套至少 7 处复制且语义微差（fallback 行为不一） | upstream/http.ts、pixiv-feed、booru、reverse-search、social、site-identity、tag-suggest | 平铺模块无共享 util 约定 | 修 bug 需多点同步 | 上游解析层 | 低 | 统一从 upstream/http.ts 导入 | P3 |
| ~~**TD-33**~~（✅ PR #117） | `.tmp-kami-root.txt` 误入库：内容为本机用户临时路径（`C:\Users\...\Temp\kami-auth-test`），全仓无引用，.gitignore 无规则（已核验仍被 git 追踪） | 仓库根（commit 67609b8 带入） | 某次 auth 测试的 KAMI_ROOT 记录被顺手提交 | 仓库泄露本机路径；卫生问题 | 仓库 | 极低 | git rm + .gitignore 补规则 | P3 |
| ~~**TD-34**~~（✅ 本地修复：startup.sh 未被 git 追踪，删 preview.mjs 引用即可） | startup.sh（平台 revive 契约残留）引用已删除的 scripts/preview.mjs | startup.sh:4 | Grok 平台退出后未更新 | `|| true` 兜底不致命，但契约脚本过时 | 平台 revive 场景【需要确认是否仍需保留】 | 极低 | 更新为探测 + npm run dev 直启，或删除 | P3 |
| ~~**TD-35**~~（✅ PR #117：徽章 + .env.example；storage.md:129 复核路径准确不改；VITE_STUN_URLS 已无消费方未加入） | 文档/配置漂移杂项：README 徽章（0.8.60/Next 15/TS 5.7 vs 实际 Next 16.3/TS 6+tsgo，已核验未更新）；.env.example 缺 DATABASE_URL/BETTER_AUTH_SECRET/KAMI_ROOT/PUPPETEER_EXECUTABLE_PATH/VITE_STUN_URLS；storage.md:129 旧路径 | README.md:12,16,17、.env.example、docs/storage.md | 依赖大版本升级后未回写周边 | 部署者/接手者被误导 | 首次部署体验 | 低 | 三处一并更新（docs/08/10 本次已对齐） | P3 |
| ~~**TD-36**~~（✅ PR #119，单测锁） | fanbox 列表缓存键 payload 不含 hideAi（pixiv 系含）——参数集不对称，未来给 fanbox 加 AI 过滤会静默串缓存 | source-cache.server.ts:46-51 | 缓存键字段按 op 手工列举 | 潜在缓存污染 | fanbox 过滤演进 | 极低 | 键 payload 统一包含全部过滤开关 | P3 |
| **TD-37**（新） | site-identity.ts 本地 decodeHtmlEntities 仅替换三种实体且源码写法疑似自替换恒等【需要确认是否转义丢失的回归】，与 utils.ts 版本分叉 | site-identity.ts:160-165 | 双实现漂移 | 画师名解析不完整 | 画师昵称显示 | 极低 | 删除本地版改用 utils 版 | P3 |
| ~~**TD-38**~~（✅ S10~S11 批次修复） | vault 页 refresh() 内 listServerVault 无 try/catch（listVault 有）→ 远端异常冒泡 unhandled rejection | vault.tsx:55-76 | 防御不对称 | 控制台噪音、合并中断 | 纸匣页 | 极低 | 补 try/catch 退回本地目录 | P3 |
| ~~**TD-39**~~（✅ PR #119） | ranking-store open 失败后无冷却：坏目录时每次 GET /api/rankings 都重试 open + warn | ranking-store.server.ts:128-137 | 失败路径无退避 | 日志噪音 + 无谓 IO | 榜单接口 | 极低 | 失败后置 null 冷却 N 分钟 | P3 |
| **TD-40**（新） | inflightMedia 单飞绑定**首个**调用方的 signal 与 cookie（注释自认单用户假设）；访客层开放后访客与登录用户并发同 URL 可能拿到错误凭据结果或被共享 abort 掐断 | upstream/media.ts:99-126 | 单飞设计早于多凭据形态 | 错凭据取图（低概率）/中断传播 | 访客+主人并存场景 | 中 | 单飞键加入凭据指纹；abort 影响面文档化 | P3 |

**量化概览（2026-09-12）**：TD-00~22 共 23 项全部闭环（TD-20~22 = bd1f28c，同日）；**待清 5 项 = TD-30/31/32/37/40（P3 × 5，TD-40 含并发语义待设计）**；安全侧待修项清零（SEC-14 已修、SEC-15 ✅ PR #117）。无 P0/P1。
