# 16 — 优化与演进路线

> 原则：尊重「单用户自托管」的产品定位，不做平台化过度设计；优先偿还「低成本高回报」债务；每阶段可独立交付。基线 `main@75e7de0`（2026-09-12 全量重推导）。

## 已完成批次（历史存档）

**短期 S1~S9（2026-09-10/11 全部 ✅）**：S1 访问闸+LAN 令牌（SEC-01/02/07）· S2 glob 测试+CI 四门禁 · S3 Grok 平台退出清尾 · S4 锁文件统一 · S5 vault 原子写+Konachan 盲区 · S6 缓存水位 · S7 错误可观测性（b4cf4e9：502/400 分类 + `[模块:操作]` 日志）· S8 Docker 默认开账号 · S9 快照全表枚举+FK 拓扑恢复（7e6169a+6ea86de）。

**中期 M1~M7（2026-09-11 全部 ✅）**：M1 拆 upstream/ 六模块 · M2 凭据收敛服务端侧（备份 v2+KEK 密文+queryKey 指纹）· M3 同步分段 LWW · M4 tsconfig/页竞态/删依赖 · M5 纸匣分批渲染 · M6 队列三件套（并发可配/退避重试/Web Locks 单执行者）· M7 E2E 框架化进 CI（b6b6305）。

**第四批（2026-09-12，bd1f28c ✅）**：TD-20 访客绑定链路（三路由开 guest + relay 快速失败）· TD-21 凭据同步守卫（脏标记+mergeCredentialsOnPull+补推）· TD-22 快照失败行隔离 · SEC-11 auth rateLimit(60s/20) · SEC-12 sync 同站校验；配套单测 5 例 + e2e 断言。

## 短期（下一批，0–3 个月）：小缺陷清偿与工程卫生

| # | 事项 | 对应 | 备注 |
| --- | --- | --- | --- |
| ~~S10~~ ✅ | 关闭 legacy 明文同步写路径（410） | SEC-14 / TD-25 | 已修：POST legacy 410，GET 读回退保留 |
| ~~S11~~ ✅ | 功能 bug 三连：user 页关注乐观更新键（TD-23，已核验）、danbooru safeMode 标签挤掉（TD-24）、vault 页远端异常未捕获（TD-38） | TD-23/24/38 | 已修（含 TD-24 单测锁） |
| ~~S12~~ ✅ | 工程卫生批：git rm .tmp-kami-root.txt + ignore（TD-33）、startup.sh 更新或删除（TD-34）、README 徽章/.env.example/storage.md 对齐（TD-35）、.dockerignore 补 kami.config.json（SEC-15） | TD-33/34/35、SEC-15 | 已修（PR #117） |
| ~~S13~~ ✅ | e2e 隔离加固：本地复用 server 时断言 KAMI_ROOT 指向临时根 | TD-26 | 已修（PR #117：改专用 8090 端口，复用只可能命中隔离 server） |
| ~~S14~~ ✅ | .dockerignore/镜像双保险：danbooru 试走 undici 池（挑战页才降级 curl） | PER-10 | 已修（PR #120）：JSON 通道池优先+自动降级，真网 A/B ~1.9x；图片通道维持 curl（残余半面）；PER-13 拆包已随 M8 批次完成（PR #119） |

**完成判定**：12 号文档无「待修」项；`pnpm test` + e2e 全绿含新增回归锁。

## 中期（3–12 个月）：一致性、超时与测试二期

| # | 事项 | 对应 |
| --- | --- | --- |
| ~~M8~~ ✅ | 出站健自动化：JSON 路统一 AbortSignal.timeout(45s)（TD-28）；双层缓存 TTL 单一常量 + 水位计数外移（TD-27）；save-work fetchBlob 补退避重试（TD-29） | TD-27/28/29 | 已修（PR #119，另收 TD-36/39 + PER-13） |
| M9 | 测试二期：c8 覆盖率基线；组件测试（RTL）覆盖乐观更新家族（TD-23 类 bug 的系统性防线）；queryKey/setQueryKey 一致性 lint 规则 | 10 号矩阵 P2 |
| M10 | 模块边界：src/lib 按域分目录（upstream/auth/storage/sync/ui-support）+ eslint 边界；as* 四件套合并（TD-32）；AppShell 动态 import 首页（PER-13）；sweepCache 分批 yield（PER-11） | TD-32、PER-11/13 |
| M11 | 磁盘治理二期：rankings 保留期上限 + vault 容量视图（TD-31）；大文件缓存口径（PER-8，先采样）；pixivUser 翻页缓存（PER-9） | TD-31、PER-8/9 |
| M12 | 数据库小整备：exported_at 统一 bigint（TD-30）；快照 jsonb 白名单改按 information_schema 列类型枚举 | TD-30、06 号已知问题 6 |

## 长期（1 年以上）：演进方向（触发条件制）

| 方向 | 内容 | 触发条件 |
| --- | --- | --- |
| 多用户/家庭化 | workbox 鉴权、per-user vault 隔离、配额 | 真实多用户需求出现前不动（单例设计省成本） |
| 可观测性 | pino 结构化日志 + 请求 id 贯穿 UI→API→上游；上游健康面板 | R-01 排查成本明显上升时 |
| 上游适配韧性 | 解析器快照流水线（定期抓真实响应脱敏入库）；站点能力探测降级面板 | R-01 第二次大改版后 |
| 数据库演进 | PGlite → better-sqlite3 或常驻 Postgres（含多实例锁，R-12） | 数据量/写入频率显著增长 |
| 部署形态收敛 | 官方形态=本机+Docker；Vercel 路径正式移除（.vercel 残留清理） | 部署需求出现时 |
| DevOps | GHCR 镜像自动构建、compose 一键升级、备份定时提醒 | 用户群扩大 |
| 凭据终局 | localStorage 凭据收敛到服务端 HttpOnly（SEC-03 残余） | 多设备使用成为常态时 |

---

## 附：软件质量模型评分（ISO/IEC 25010 + 扩展维度，2026-09-12 全量重推导重评）

| 维度 | 分 | 依据 |
| --- | --- | --- |
| Functional Suitability | 8.5 | 21 项功能全真实可用；访客层含绑定链路（bd1f28c）补齐；扣分：TD-23/24 两个可见小 bug |
| Performance Efficiency | 8.5 | undici 池+三层缓存+双闸+车道+单飞；扣分：新增 PER-8~14 观察项（danbooru curl 通道、大画师翻页放大、大文件不缓存） |
| Compatibility | 7 | 双视口+iOS 旧 WebKit 兜底+FS Access 降级；扣分：node:sqlite 实验性收窄运行时面、降级未文档化 |
| Usability | 8.5 | 中文 UI、三步向导、投屏登录、纸感动效一致 |
| Reliability | 8 | 原子写/熔断/重试/水位/失败行隔离（bd1f28c）齐备；扣分：快照 0.5-30s 丢写窗口、LWW 时钟偏移 |
| Security | 7.5 | 12 路由全闸+同站校验+rateLimit+KEK/备份加密+指纹化；残余：SEC-06/08 观察、localStorage 明文（长期） |
| Maintainability | 7.5 | 类型纪律+注释文化+判别联合穷尽；扣分：UI 巨文件（settings 827）、as* 七处复制、平铺无子域边界 |
| Portability | 7.5 | 本机+Docker 双形态完整；扣分：Vercel 残留待正式移除、实验性 API 栈 |
| 架构设计 | 8 | BFF 单漏斗+薄壳迁移+三层缓存+双层闸都是好决策；扣分：AppShell 反向依赖首页、队列与浏览器存储强耦合 |
| 数据库设计 | 7 | 双后端同构+快照兜底+分段 LWW；扣分：exported_at 类型不一致（TD-30）、jsonb 白名单硬编码、legacy 明文落盘、无 FK 靠应用层 |
| 代码质量 | 7 | 见 11 号：高于个人项目均值，死于细节（工具复制、静默吞错尾部、平台遗留） |
| 工程化 | 7.5 | CI 五门禁（verify/docker/e2e）+glob 测试+pnpm 收口+修复即带回归锁（bd1f28c 示范） |
| 测试 | 7 | 49 文件 173 个顶层用例（提交口径 234 全绿，含子测试）+3 条 E2E+4 手动 QA；扣分：无覆盖率数字、无组件测试 |
| 文档 | 8.5 | CHANGELOG 优秀+专题文档实用+本套工程文档全量重推导对齐 |
| 可扩展性 | 7.5 | 新站点/新 op 路径清晰（穷尽检查）；上游已模块化 |

**项目综合健康度：★★★★（4 / 5）**

一句话评分依据（2026-09-12）：**四批修复把安全（4→7.5）、工程化（5→7.5）、可靠性的「接线与流程」欠账基本清完，产品完成度与性能工程保持小团队商用水准；当前短板收敛为散点小缺陷（TD-23~40，无 P0/P1）+ 一个待修安全项（SEC-14）+ 新识别的前沿技术栈组合风险（R-11）——全部是明确工单而非系统性风险。**
