# 16 — 优化与演进路线

> 原则：尊重「单用户自托管」的产品定位，不做平台化过度设计；优先偿还「低成本高回报」债务；每阶段可独立交付。

## 短期（0–3 个月）：止血与门禁

| # | 事项 | 对应 | 预估量级 |
| --- | --- | --- | --- |
| S1 | 数据面 API 全量接线 `runAuth`；auth 关闭态用启动随机令牌（终端打印 + 首次访问引导）；login-browser 响应剥离明文凭据 | SEC-01/02/07、TD-00 | 一个 PR，现成函数 |
| S2 | 测试自动发现替代手工清单；建 CI（typecheck+test+lint+build） | TD-02/03 | 半天 |
| S3 | 迁移清尾：删 Vite/Nitro 死代码与失效工具（check:auth、preview.mjs、server/middleware、multiplayer、nf3 override）；修 manifest 死链 | TD-04 | 一个 PR + 回归冒烟 |
| S4 | 锁文件统一（提交 pnpm-lock，Dockerfile 改 corepack） | TD-06 | 半天 |
| S5 | vault put / 文件夹写改 tmp+rename 原子；Konachan 200+HTML 盲区补判 | TD-08、TD-11 | 小 PR |
| S6 | 缓存清理：启动后台扫描过期文件 + 总量水位 | TD-07、PER-1 | 小 PR |
| S7 | 错误可观测性最小版：吞异常处加 `[module:op] console.warn`；/api/source 区分 502/400 | 11.4、07.3 | 顺手 |
| S8 | Docker 形态 VITE_AUTH_ENABLED 改 build ARG；README 补三形态能力矩阵 | TD-17 | 半天 |

**短期完成的判定**：LAN 内未授权设备无法访问任何数据面端点；`pnpm test` 自动覆盖全部测试文件；CI 在每个 PR 上红灯即挡。

## 中期（3–12 个月）：结构化与凭据治理

| # | 事项 | 说明 |
| --- | --- | --- |
| M1 | **拆解 upstream.server.ts** → `adapters/pixiv/ fanbox/ booru/`（每站 list/detail/social/suggest 独立文件）+ `media-proxy.ts` 独立。dispatch 签名不变，上层零改动 | TD-01；配合解析器单测补齐（10.3 矩阵 P0 项） |
| M2 | **凭据收敛**：HttpOnly cookie 成唯一服务端凭据来源（fetchSchema 去掉 cookie 字段，服务端从 cookie 读）；queryKey 改账号 id；备份/同步 payload 加口令加密（旧备份自动迁移） | SEC-03/04、TD-10 |
| M3 | **同步协议升级**：user_sync 分段 LWW（settings/vault/lexicon/history 各带 exportedAt）+ payload zod 校验 | TD-09/18 |
| M4 | tsconfig 覆盖 scripts/；补 vault/rankings 页竞态防护；删未用依赖 | TD-05/15/16 |
| M5 | 纸匣页性能：objectURL 生命周期治理 + 分页/虚拟化 | PER-3 |
| M6 | 队列体验：可配置并发（>1）、失败自动重试（指数退避）、跨标签页共享（BroadcastChannel） | 04/FP-3 异常项 |
| M7 | E2E 框架化：把 qa-\* 收编进 playwright.config，关键路径（浏览→详情→入队→纸匣）进 CI（本地 runner） | 10.3 |

## 长期（1 年以上）：演进方向

| 方向 | 内容 | 触发条件 |
| --- | --- | --- |
| 多用户/家庭化 | 若需求从单用户走向家庭多账号：workbox 鉴权、per-user vault 隔离、配额 | 出现真实多用户需求前不动（当前所有单例设计都以此省成本） |
| 可观测性 | 结构化日志（pino）+ 请求 id 贯穿 UI→API→上游；上游健康面板（每站最近错误/延迟） | 上游对抗（R-01）排查成本明显上升时 |
| 上游适配韧性 | 解析器快照测试流水线（定期抓真实响应脱敏入库）；站点能力探测降级面板 | R-01 第二次大改版后 |
| 数据库演进 | 若 PGlite 快照窗口不可接受：迁移到 better-sqlite3（native，需装权限）或常驻 Postgres | 数据量/写入频率显著增长 |
| 部署形态收敛 | 官方支持形态收敛为「本机 + Docker」两条；Vercel 路径正式移除或改造（serverless 与 .data 假设冲突） | 出现部署需求时 |
| DevOps | 镜像自动构建（GHCR）、compose 一键升级脚本、备份定时提醒 | 用户群扩大 |

## 评分与路线的对应

综合健康度 ★★★☆（3.5/5，详见下）——短期项全部完成后安全维度可从 4 分升至 7 分，综合可上 ★★★★；中期 M1/M2 完成后可维护性与安全性进入良性区间。

---

## 附：软件质量模型评分（ISO/IEC 25010 + 扩展维度，满分 10）

| 维度 | 分 | 依据 |
| --- | --- | --- |
| Functional Suitability 功能完备 | 8.5 | 24 项功能全部真实可用（CHANGELOG 逐条兑现）；扣分：Docker 形态功能矩阵未对齐（TD-17） |
| Performance Efficiency | 8 | undici 池 + 三层缓存是同类项目少见的水准（详情页毫秒级有实测记录）；扣分：PER-2/3 重渲染与无虚拟化 |
| Compatibility 兼容 | 7 | 双视口适配 + FS Access 降级路径齐备；扣分：PWA manifest 死链、非 Chromium 浏览器功能降级未文档化 |
| Usability 易用 | 8.5 | 中文标签、三步向导、投屏登录（零贴 cookie）、纸感动效一致性好 |
| Reliability 可靠 | 7 | 崩溃安全设计（快照/原子写/熔断）意识强；扣分：0.5-30s 丢写窗口、vault 非事务、队列无重试 |
| Security 安全 | **4** | 架构件（runAuth/隔离/bearer）已备但未接线 + 凭据明文多落点 + LAN 默认暴露；详见 12 文档 |
| Maintainability 可维护 | 6 | 类型纪律与注释好；扣分：上帝模块、三代遗留、无 CI、测试漂移 |
| Portability 可移植 | 6.5 | 三形态齐备但只有本机形态完整；双锁文件；部分工具 Linux-only |
| 架构设计 | 7.5 | BFF 单漏斗 + 三层缓存 + 薄壳迁移都是好决策；扣分：鉴权层脱线、上游模块集中 |
| 数据库设计 | 7 | 双后端同构迁移、快照兜底聪明；扣分：vault 非事务、LWW 覆盖、无外键关联靠应用层 |
| 代码质量 | 7 | 见 11 文档：高于个人项目均值，死于细节（吞异常无日志、死代码、重复实现） |
| 工程化 | 5 | 无 CI、测试清单漂移、失效工具仍在文档/脚本里被引用 |
| 测试 | 5.5 | 单测覆盖解析/算法层 + 场景 QA 质量高；无 E2E 框架化、无覆盖率、10 文件漏跑 |
| 文档 | 7.5 | CHANGELOG 优秀、专题文档（storage/upstream 等）实用；缺架构级文档（本套补齐） |
| 可扩展性 | 7 | 新站点路径清晰（判别联合强制穷尽）；上游模块集中是主要阻力 |

**项目综合健康度：★★★☆（3.5 / 5）**

一句话评分依据：**产品完成度与性能工程达到小团队商用水准（功能 8.5 / 性能 8），但安全（4）与工程化（5）拖了后腿**——而这两项恰好都是「接线与流程」问题而非「设计能力」问题，修复成本低，这是本项目最值得乐观的地方。
