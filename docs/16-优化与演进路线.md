# 16 — 优化与演进路线

> 原则：尊重「单用户自托管」的产品定位，不做平台化过度设计；优先偿还「低成本高回报」债务；每阶段可独立交付。
> **2026-09-11 复核 + 同日清偿**：短期 S1-S9、中期 M1-M5 已全部完成（状态逐项标注）；剩余待办为 M6/M7 与大版本维护升级（另立 PR）。

## 短期（0–3 个月）：止血与门禁

| # | 事项 | 对应 | 状态（2026-09-11） |
| --- | --- | --- | --- |
| S1 ✅ | 数据面 API 全量接线访问闸；auth 关闭态用启动随机令牌（终端打印 + 首次访问引导）；login-browser 响应剥离明文凭据 | SEC-01/02/07、TD-00 | ✅ 90a9050（withDataPlane + lan-token 三通道） |
| S2 ✅ | 测试自动发现替代手工清单；建 CI（typecheck+test+lint+build） | TD-02/03 | ✅ 90a9050（glob + ci.yml 四门禁） |
| S3 ✅ | 迁移清尾：删 Vite/Nitro 死代码与失效工具 | TD-04 | ✅ Grok 平台完全退出（残留 .grok/、startup.sh、.vercel/ 顺手清） |
| S4 ✅ | 锁文件统一（提交 pnpm-lock，Dockerfile 改 corepack） | TD-06 | ✅ |
| S5 ✅ | vault put / 文件夹写改 tmp+rename 原子；Konachan 200+HTML 盲区补判 | TD-08、TD-11 | ✅ |
| S6 ✅ | 缓存清理：写入惰性扫描 + 总量水位 | TD-07、PER-1 | ✅ cache-watermark（media 2GB / source 512MB） |
| S7 | 错误可观测性最小版：吞异常处加 `[module:op] console.warn`；/api/source 区分 502/400 | 11.4、07.3 | **未做**（无对应提交，保持原优先级） |
| S8 ✅ | Docker 形态 VITE_AUTH_ENABLED 默认 true；README 对齐 | TD-17 | ✅ 8fa2245 |
| S9 ✅ | 快照表清单补 user_sync_segments / user_sync_meta + 重启恢复回归测试（或改为「除 _migrations 外全表」模式绝后患） | TD-19 | ✅ 7e6169a + 6ea86de：快照改 public 全量动态枚举，恢复按 FK 依赖拓扑排序（字母序会静默丢 session/account），三条回归测试锁住 |

**短期完成的判定**：LAN 内未授权设备无法访问任何数据面端点 ✅；`pnpm test` 自动覆盖全部测试文件 ✅；CI 在每个 PR 上红灯即挡 ✅；**分段同步数据跨重启存活 ✅（S9，2026-09-11 修复并回归覆盖）**。

## 中期（3–12 个月）：结构化与凭据治理

| # | 事项 | 状态（2026-09-11） |
| --- | --- | --- |
| M1 ✅ | **拆解 upstream.server.ts** → 已落地为 `src/lib/upstream/`：pixiv / fanbox / booru-sites / media / dispatch / http 共享层；upstream.server.ts 为桶，上层零改动。解析器固定样本测试 7 例（mapping.test.ts），TD-11 konachan HTML 盲区一并修掉（2026-09-10） | TD-01 ✅ |
| M2 ✅ | **凭据收敛（服务端侧）**：备份文件 v2 口令加密（PBKDF2+AES-GCM）、账号同步设置段 KEK 密文、queryKey 凭据指纹化 + browse-cache v2（旧污染存储读到即删） | SEC-03/04、TD-10 ✅（localStorage 明文收敛仍属长期） |
| M3 ✅ | **同步协议升级**：user_sync_segments 分段 LWW + validateSegment 分段校验 + 旧单行兼容（0003 迁移已应用） | TD-09/18 ✅（TD-19 分段表快照缺口已于 2026-09-11 修复） |
| M4 ✅ | tsconfig 覆盖 scripts/；vault/rankings 页竞态防护（请求令牌）；删未用依赖（recharts 等 3 个） | TD-05/15/16 ✅（TD-15 残余 4 个零引用依赖已于 2026-09-11 移除，cmdk/vaul/react-resizable-panels/jose） |
| M5 ✅ | 纸匣页性能：分批渲染（首批 60 / 续 40）+ revoke 复核 | PER-3 ✅ |
| M6 | 队列体验：可配置并发（>1）、失败自动重试（指数退避）、跨标签页共享（BroadcastChannel） | 未做 |
| M7 | E2E 框架化：把 qa-\* 收编进 playwright.config，关键路径（浏览→详情→入队→纸匣）进 CI（本地 runner） | 未做 |

## 长期（1 年以上）：演进方向

| 方向 | 内容 | 触发条件 |
| --- | --- | --- |
| 多用户/家庭化 | 若需求从单用户走向家庭多账号：workbox 鉴权、per-user vault 隔离、配额 | 出现真实多用户需求前不动（当前所有单例设计都以此省成本） |
| 可观测性 | 结构化日志（pino）+ 请求 id 贯穿 UI→API→上游；上游健康面板（每站最近错误/延迟） | 上游对抗（R-01）排查成本明显上升时 |
| 上游适配韧性 | 解析器快照测试流水线（定期抓真实响应脱敏入库）；站点能力探测降级面板 | R-01 第二次大改版后 |
| 数据库演进 | 若 PGlite 快照窗口不可接受：迁移到 better-sqlite3（native，需装权限）或常驻 Postgres | 数据量/写入频率显著增长 |
| 部署形态收敛 | 官方支持形态收敛为「本机 + Docker」两条；Vercel 路径正式移除或改造（serverless 与 .data 假设冲突） | 出现部署需求时 |
| DevOps | 镜像自动构建（GHCR）、compose 一键升级脚本、备份定时提醒 | 用户群扩大 |

## 评分与路线的对应

综合健康度 ★★★★（4/5，2026-09-11 复核并更新，详见下）——短期项 S1-S9 全部完成（S9 于 2026-09-11 闭环），安全维度已从 4 分升至 7.5；可靠性与功能完备随 S9 各 +0.5；剩余短板为 S7 可观测性与中期 M6/M7。

---

## 附：软件质量模型评分（ISO/IEC 25010 + 扩展维度，2026-09-11 复核口径）

| 维度 | 分 | 依据 |
| --- | --- | --- |
| Functional Suitability 功能完备 | 8.5 | 24 项功能全部真实可用；Docker 形态已对齐（TD-17 ✅）；扣分：TD-19 使「账号同步」在重启后名存实亡 |
| Performance Efficiency | 8.5 | undici 池 + 三层缓存 + 媒体车道（点击→跳转从 10s 降到亚秒）+ 进度节流 + 分批渲染；扣分：PER-6/7 维持现状 |
| Compatibility 兼容 | 7 | 双视口适配 + FS Access 降级路径齐备；扣分：非 Chromium 浏览器功能降级未文档化 |
| Usability 易用 | 8.5 | 中文标签、三步向导、投屏登录（零贴 cookie）、纸感动效一致性好 |
| Reliability 可靠 | 7.5 | 崩溃安全设计（快照/原子写/熔断/水位）意识强且已落地；扣分：0.5-30s 丢写窗口、TD-19 快照缺口、队列无重试 |
| Security 安全 | **7.5** | 数据面统一访问闸 + Fetch-Metadata 同站校验 + 备份/同步加密 + 凭据指纹化均已接线；残余：SEC-06（http 明文 cookie，LAN 取舍）、localStorage 明文（长期） |
| Maintainability 可维护 | 7.5 | 类型纪律与注释好；上游层已拆分；扣分：横向依赖无 lint 边界、TD-14 死注释 |
| Portability 可移植 | 7.5 | 本机 + Docker 双形态完整（锁文件已收口）；扣分：Vercel 遗留待正式移除 |
| 架构设计 | 8 | BFF 单漏斗 + 三层缓存 + 薄壳迁移 + 两层并发闸（media-lane/media-gate）都是好决策且鉴权已接线；扣分：队列与浏览器存储强耦合 |
| 数据库设计 | 7.5 | 双后端同构迁移、快照兜底、分段 LWW；扣分：TD-19 快照清单滞后、无外键关联靠应用层 |
| 代码质量 | 7 | 见 11 文档：高于个人项目均值，死于细节（吞异常无日志、死代码、重复实现） |
| 工程化 | 7 | CI 四门禁 + glob 测试 + pnpm 收口；扣分：S7 可观测性未做、qa-* 未进 CI |
| 测试 | 6.5 | 49 个测试文件（scripts 33 例 + lib 218 例）全量运行 + 上游固定样本回归；扣分：无组件/E2E 框架化、无覆盖率 |
| 文档 | 8 | CHANGELOG 优秀、专题文档实用、本套工程文档齐且经三批修复回写验证 |
| 可扩展性 | 7.5 | 新站点路径清晰（判别联合强制穷尽）；上游已模块化 |

**项目综合健康度：★★★★（4 / 5）**

一句话评分依据（2026-09-11）：**三批修复把「接线与流程」类欠账（安全 4→7.5、工程化 5→7）基本清完，产品完成度与性能工程保持小团队商用水准；当前短板收敛为两个具体点——S7 可观测性、M6/M7 队列与 E2E（TD-19 快照缺口已于 2026-09-11 修复）——均为明确工单而非系统性风险。**
