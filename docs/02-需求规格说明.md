# 02 — 需求规格说明（逆向还原）

> 本项目无正式需求文档；以下功能需求由页面、路由、API 与数据结构逆向还原。CHANGELOG 是事实上的需求变更记录（Keep a Changelog 格式，每条对应 PR）。

## 2.1 系统解决的问题

用户在浏览器直接访问图站有三类痛点：**访问不稳定**（Cloudflare 拦截、直连超时，需代理）、**凭据管理繁琐**（手动贴 Cookie 易失效）、**收藏无处安放**（原图散落、跨设备不同步）。Kami 纸匣的解法是把「访问源站」整体搬到用户自己的服务端（带代理、缓存、凭据），把「收藏」落成本地文件夹 + 应用内归档 + 账号同步三层。

## 2.2 业务角色

| 角色 | 定义 | 依据 |
| --- | --- | --- |
| 访客（未登录任何账号） | 浏览 Pixiv 全年龄榜单、三个 booru 站帖子、标签搜索、以图搜图 | README.md:50；upstream 可选 cookie 的端点 |
| 图站登录用户 | 在访客能力之上：Pixiv 推荐/关注流/R-18 榜/红心收藏关注、FANBOX 首页/订阅/付费内容 | README.md:51 |
| 应用账号用户 | 图站登录用户之上：设置跨浏览器/设备同步（纸匣自己的邮箱密码账号） | CHANGELOG「应用账号」条目 |
| 运行者（部署者） | 配置代理、限速、数据目录、备份恢复；即用户本人（单用户自托管是设计前提） | kami.config.json、settings 页 |

无 RBAC、无多租户——单用户自托管是显式设计前提（登录中转全局单例等实现均以此为基础）。

## 2.3 功能需求清单

| 编号 | 功能 | 角色 | 入口 | 核心逻辑 | 状态 |
| --- | --- | --- | --- | --- | --- |
| FR-01 | 多源浏览信息流 | 访客 | `/`（首页三 tab） | pixiv 榜单(9 mode)/推荐/关注、fanbox 首页/订阅/标签、booru recent/hot/日周月榜；每页 50 张分页 | 已实现 |
| FR-02 | 标签搜索 | 访客 | `/` 搜索框 | 空格分多 tag；粘贴作品/画师链接自动解析跳转（parse-input.ts）；booru 显示中文标签（词表三层合并） | 已实现 |
| FR-03 | 搜索联想 | 访客 | `/` 搜索框 | 本地（已保存>最近）+ 远端 tagSuggest 合并前 10 条，200ms 防抖 | 已实现 |
| FR-04 | 作品详情 | 访客 | 卡片点击 → `/work/[source]/[id]` | 多页大图/附件、Statistics、booru 合集引用、相关推荐、浏览历史自动记录 | 已实现 |
| FR-05 | 合集浏览 | 访客 | `/pool/[site]/[id]` 或粘贴 pool 链接 | booru pool 整包查看、整包下载/入纸匣 | 已实现 |
| FR-06 | 画师/创作者页 | 访客 | `/user/[id]`、`/creator/[id]` | Pixiv 置顶+60 条分页无限滚动；FANBOX 游标分页 | 已实现 |
| FR-07 | R-18 / AI 过滤 | 访客 | 浏览页顶部开关 | safeMode（全年龄）与 hideAi 参与 queryKey、源站参数（rating:g、ai_type=1）与本地过滤（xRestrict/sl/isMasked/BLOCKED_TAGS）双重生效 | 已实现 |
| FR-08 | 以图搜图 | 访客 | `/search`（拖拽/粘贴/选文件） | 浏览器缩图（≤768px、q0.82 JPEG）→ 三引擎并发 → 按引擎分组展示，风控组单独标注，命中链接反解为站内跳转 | 已实现 |
| FR-09 | 图站登录（中转） | 图站用户 | 设置→账号 / 新手向导 | 无头 Chrome 打开官方登录页，JPEG 帧投屏到应用内 canvas，鼠标键盘 IME 事件回传，轮询收割 PHPSESSID/FANBOXSESSID | 已实现 |
| FR-10 | 图站登录（手贴） | 图站用户 | 设置→账号 | parseCookieDump 支持 Cookie-Editor JSON / Netscape cookies.txt / 裸值 | 已实现 |
| FR-11 | 多账号切换 | 图站用户 | 顶栏账号下拉 | 每站最多 8 套账号，切换即换 queryKey 缓存键；身份自动探测（whoami） | 已实现 |
| FR-12 | 红心/收藏/关注 | 图站用户 | 卡片右键 / 详情页 | 乐观更新 + 失败回滚；Pixiv CSRF token 自动获取（8min 缓存，403 重取重试一次） | 已实现 |
| FR-13 | 下载 / 收入纸匣 | 访客 | 卡片按钮 / 详情页 | 一律 enqueueWork 入队（80 条上限），单并发串行；纸匣优先写用户文件夹（FS Access API），否则应用内 `.data/vault`；首页 SHA-256 巡检 | 已实现 |
| FR-14 | 队列管理 | 访客 | `/queue` | 进度/失败/重试/清除；刷新后 running 复位为 queued 续跑 | 已实现 |
| FR-15 | 纸匣管理 | 访客 | `/vault` | 应用内目录 + 浏览器 IDB + 用户文件夹三方合并展示；文本/站点/作者过滤；导出/删除 | 已实现 |
| FR-16 | 浏览历史 | 访客 | `/history` | 作品 90 天（无条数上限，解析保护 50,000）+ 作者 400 条；站点过滤 | 已实现 |
| FR-17 | 热榜归档回看 | 访客 | `/rankings` | booru 日/周/月榜首屏自动存档（SQLite，不设上限），按站/日期回看、清理 | 已实现 |
| FR-18 | 动图播放/保存 | 访客 | 卡片/详情页 | Ugoira zip 拉取（3 次重试退避）→ fflate 解帧 → canvas 循环播放；保存时 gifenc 编码 GIF（失败回退存 zip） | 已实现 |
| FR-19 | 应用账号 | 应用账号用户 | 设置→账号 | 邮箱密码注册/登录（Better Auth）；登录后设置自动同步服务端（4s 防抖推送，5s 恢复静默窗） | 已实现 |
| FR-20 | 备份导出/导入 | 运行者 | 设置→备份 | BackupFile JSON（设置/账号/纸匣目录/词表/历史，不含像素）≤2MB；不打包原图 | 已实现 |
| FR-21 | 代理配置 | 运行者 | 设置→代理 | http/https/socks4/5/5h 校验、探测（curl 打 pixiv 15s）、保存即热生效（env+config+data 三处） | 已实现 |
| FR-22 | 缓存刷新 | 访客 | 榜单「刷新」按钮 | `fresh:true` 跳过浏览器/Next 两层缓存重拉源站并 revalidateTag | 已实现 |
| FR-23 | 标签词表维护 | 运行者 | 设置→词表 | 用户中文译文增删（优先级最高）；booru 词频目录（8000 上限）浏览 | 已实现 |
| FR-24 | 主题切换 | 访客 | 顶栏 | washi 等纸感主题 + 明暗外观；首帧 bootstrap 脚本防 FOUC | 已实现 |

## 2.4 非功能需求评估（按实现推断）

| 维度 | 需求（从实现反推） | 达成情况 |
| --- | --- | --- |
| 性能 | 首屏先画浏览器缓存（同步 hydrate）；详情页打开毫秒级（undici 池预热后 0.2-0.4s，CHANGELOG Unreleased） | 良好；剩余风险见 13 文档（队列重渲染、纸匣页无虚拟化） |
| 可用性 | 断网/上游失败时三层缓存兜底；上游 5xx 与参数错误统一 400（可改进） | 中；错误信息对用户偏粗糙 |
| 稳定性 | 强杀进程不丢账号（JSON 快照 30s/500ms 双触发）；curl 熔断防双份超时 | 良好；PGlite 快照有 0.5-30s 窗口（自认） |
| 安全性 | 单用户自托管假设；HttpOnly 镜像 cookie；同站隔离工具已备 | **差**：数据面 API 无鉴权 + 0.0.0.0 默认暴露，见 12 文档 |
| 易用性 | 中文 UI、三步向导、悬停预览、纸感动效、`prefers-reduced-motion` 全覆盖 | 良好 |
| 可扩展性 | 新站点 = sites.ts 枚举 + upstream 映射 + 类型（见 09 指南）；op 联合类型强制穷尽 | 中上；upstream.server.ts 已成 1257 行上帝模块 |
| 可维护性 | 文件头注释规范（AGENTS.project.md 强制）、strict TS、44 个单测 | 中；无 CI、测试清单漂移、三代框架遗留并存 |
| 兼容性 | 桌面鼠标优先 + 移动端 390×844 适配（browser-smoke 双视口验证）；FS Access API 不可用时降级 | 良好 |
| 可测试性 | 纯函数抽离（masonry-flow、migration-plan 等）；qa-\* Playwright 场景 | 中；无 E2E 框架化、无覆盖率 |
| 可部署性 | 本机/Docker/Vercel 三形态；standalone 输出 | 中；Vercel 形态遗留且与本地数据假设冲突 |

## 2.5 需求完整性缺口

| 缺口 | 说明 | 依据 |
| --- | --- | --- |
| 需求来源不可追溯 | 无 issue/PR 之外的任何需求产物；CHANGELOG 只记「做了什么」不记「为什么」 | 全仓库 |
| 隐式需求散落代码 | 单用户假设（登录中转单例、队列无并发）、LAN 暴露（0.0.0.0）、明文凭据（localStorage）都是「约定俗成」而非显式决策记录 | browser-login.server.ts:89-93、kami.config.example.json:2 |
| 多设备并发场景未定义 | user_sync 整载荷 LWW，多设备同时改设置互相整份覆盖——需求层从未回答「并发编辑怎么办」 | app/api/account/sync/route.ts:65-72 |
| R-18 合规边界靠实现硬编码 | BLOCKED_TAGS 21 词、反搜丢弃 loli/shota 词组等安全规则内嵌于业务代码，无配置化、无说明出处 | booru.ts:71-93、reverse-search.ts:26 |
| 部署形态能力矩阵未对齐 | Docker 镜像烧死 `VITE_AUTH_ENABLED=false`（应用账号不可用），README 未说明该差异 | Dockerfile:16,36 vs README 功能描述 |
| Vercel 形态数据矛盾【需要确认】 | `.vercel/output` 是 Nitro 旧产物；若仍在 Vercel 部署，serverless 下 PGlite 无持久化、`.data` 不可写，纸匣/热榜功能名存实亡 | .vercel/output/config.json、ranking-store.server.ts:121-131 |
